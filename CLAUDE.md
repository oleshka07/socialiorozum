# socialio / КонтентГров — project memory (single source of truth)

> **PROCESS (read first):** This file is the canonical, current state of the project.
> 1. **READ this file before any change** (Claude Code auto-loads it each session).
> 2. **UPDATE it after every change/deploy** — edit the relevant section AND add a line to the Changelog (bottom). Keep it as *current state*, not a diary.
> 3. Detailed historical log also lives in Claude-memory `socialio-deploy.md`, but **this file wins** if they differ.

---

## 1. What it is
**socialio.rozum.one** ("КонтентГров") — multi-tenant SMM content engine (Ukrainian UI). A user gives a source (their Instagram, a transcript, an idea, RSS) → AI generates ready-to-publish posts (text + image) in the brand voice → review → schedule → publish to Telegram / Instagram / Facebook / Threads. Goal: "щоб міг кожен" (anyone, non-technical).

Owner/operator: **Oleg** (o.stepeniev@swipescape.eu). Wants autonomous work: push, find bugs, fix, deploy without manual coding.

## 2. Stack & repo
- **Node 20 + TypeScript + Fastify 5 + PostgreSQL 16 + Docker Compose.** Code in `server/` (`server/src/*.ts`, SPA in `server/public/app.html`, auth pages `auth.html`, landing `index.html`).
- Repo: **github.com/oleshka07/socialiorozum** (PRIVATE). Push over **HTTPS via gh credential helper** (the local `~/.ssh/github_oleshka07` key is scoped to another repo).
- Local dir: `D:\Claude\Claude\Projects\КонтентГров`.

## 3. Deploy & verify
- **`git push origin main` → GitHub Actions (`.github/workflows/deploy.yml`, appleboy/ssh-action) → SSH to Hetzner `178.105.185.67` → `/opt/socialio` `git pull` + `docker compose up -d --build`.** Compose project pinned `socialio`; app on 127.0.0.1:8080; nginx + certbot at https://socialio.rozum.one.
- **БЕТА:** гілка **`beta`** → `deploy-beta.yml` → `/opt/socialio-beta` → https://beta.socialio.rozum.one (PIN 2345, порт 8081, окрема БД/медіа через `COMPOSE_PROJECT_NAME=socialio-beta`+`APP_PORT` у .env бети; `TELEGRAM_WEBHOOK_OFF=1` — webhook лишається за продом, DM-фічі бота тестуються тільки на проді). **Новий цикл роботи: фічі → `beta` → Олег тестує → merge beta→main = промоушн на прод; хотфікси → main → merge у beta.** Разове розгортання бети на сервері: `server/BETA.md`.
- SSH key: `~/.ssh/rozum_hetzner`. Server pulls private repo via its own deploy key.
- **Verify a deploy:** wait box `git -C /opt/socialio rev-parse --short HEAD` == pushed commit → then `curl https://socialio.rozum.one/health`==200 + container `Up Ns` (fresh) + `curl /app | grep <marker>`. Build TS first: `cd server && npm run build` (tsc 0). For static app.html JS: extract `<script>` and `node --check`.
- **`.env` on server** (`/opt/socialio/server/.env`, chmod 600, `env_file:` in compose → needs `docker compose up -d --force-recreate --no-deps app` to re-read): editing it is **classifier-gated** — needs the user's EXPLICIT in-chat OK; I never write secret VALUES (operator pastes them).
- ⚠️ **Do NOT `git add -A`.** The working tree has the user's OWN uncommitted files: `landing/index.html`, `architecture-content-engine.md`, `research/`, `brand/`, `*.md` audits, `app-flow-architecture.md`, `google-verification-instruction.md`, `threads-holos-handoff.md`. **Stage only the specific `server/**` files (and this CLAUDE.md) you changed.**

## 4. Env / secrets (names only — never values in code/chat/git)
`DATABASE_URL`, `SESSION_SECRET`, `APP_BASE_URL`, `OPENROUTER_API_KEY`, **`OPENAI_API_KEY`** (Lite direct + gpt-image-1), **`FAL_KEY`** (FLUX images), **`GEMINI_API_KEY`** (Nano Banana images), `RESEND_API_KEY` + `RESEND_FROM=socialio <noreply@rozum.one>` (rozum.one VERIFIED → delivers to all), `GOOGLE_CLIENT_ID/SECRET` + `GOOGLE_API_KEY` (login + Drive Picker), `META_APP_ID/SECRET`, `THREADS_APP_ID/SECRET`, **`TELEGRAM_BOT_TOKEN` + `TELEGRAM_BOT_USERNAME`** (shared bot `@R_Socialio_bot`), `ADMIN_EMAILS`. All keys present/active per user (OpenAI/FAL/Gemini/Telegram added).

## 5. Architecture (server/src)
- `server.ts` — all routes + preHandler auth (cookie session, scoped to `req.user.workspace_id`; `/api/auth/*` + `/api/webhooks/*` exempt) + workers started after `listen`.
- `auth.ts` (scrypt sessions, `createWorkspaceWithDefaults`/`seedWorkspaceDefaults`, account lifecycle helpers) · `email.ts` (Resend) · `defaults.ts` (DEFAULT_SETTINGS now mostly EMPTY — brand derived from IG) · `db.ts` · `env.ts` · `log.ts`.
- `pipeline.ts` — **Lite** (`buildLitePrompt`/`generatePostsOnePass` = ONE gpt-4o call → posts, default path) + **PRO** 6-step (`STEP_ORDER=[extract_ideas,drafts,tone,format,deai,strategy]`, `executeStep`, sequential+chained), `deriveVoice`/`deriveBrandFromText`, `generateStrategy`, `adaptForChannels`, `rewritePost`. `openrouter.ts` `chat()` routes `openai/*`→api.openai.com when OPENAI_API_KEY set else OpenRouter.
- Channels: `telegram.ts`+`tgbot.ts` (shared bot webhook), `threads.ts`, `meta.ts` (FB/IG + `getRecentMedia` for voice), `publisher.ts` `publishPostToChannels` (composer + autopost; all 4 nets post WITH image).
- `media.ts` (saveMedia/MEDIA_DIR/deleteMediaFile/getThumb sharp thumbnails/convertAllHeif) · **`images.ts`** (AI image gen: openai gpt-image-1 / fal FLUX / gemini Nano Banana + `overlayHeadline` sharp+SVG text overlay).
- Sources: `rss.ts`+`rss-poller.ts`, `gdrive.ts`+`gdrive-poller.ts` (drive.file+Picker), transcribers `fireflies.ts`/`grain.ts`/`meetgeek.ts`.
- Workers (setInterval, started in `listen`): `autopost.ts` (60s), `rss-poller` (15m), `gdrive-poller` (15m), `lifecycle.ts` (6h: account purge/retention/orphan-media), boot `convertAllHeif`, `initTelegramBot`.
- **DB tables:** workspace, app_user (+last_active_at/deleted_at/inactivity_warned_at/data_purged_at), user_session, email_token, settings_block, prompt_template, rubric, strategy, source, pipeline_run, step_run, idea, post (+review/channels jsonb/media_id/image_prompt/**image_base/headline**), content_plan, plan_item, schedule_slot (+post_id/result), media_asset, telegram_config, tg_connect, telegram_publish, threads_config/_publish, meta_config, meta_publish, content_source (RSS), gdrive_config/_folder, transcription_config, llm_usage, app_log. Most reference `workspace ON DELETE CASCADE`; `app_user.workspace_id` is 1:1 (→ multi-workspace is the multi-account plan).

## 6. Feature inventory (current, all DEPLOYED & live)
- **Auth/account:** email+scrypt + Google OAuth; multi-tenant per-workspace; account lifecycle (self-delete soft+14d grace, inactivity retention, media cleanup, password/email/GDPR-export); **«🔄 Почати з чистого листа»** reset (wipes content+brand, keeps channels, re-onboards) — temporary testing aid.
- **Onboarding (IG-first, instant posts):** step 0 connect Instagram (popup OAuth, account picker if multiple) → pull ~20 IG captions → `deriveBrandFromText` (niche/voice/strategy/language, OVERWRITES on switch) → steps brand/voice/source (skippable; «Пропустити» finishes with what's there) → **progress popup (3 key-points)** → auto-generate posts **+ images** → land on Студія. No transcript needed (generate-from-brand).
- **Studio = global post pool:** Студія/Інбокс show ALL workspace final posts (`/api/posts/studio` → `Finals`), NOT tied to the active source/run — switching source does not change the list. The active run is only the generation context (Конвеєр stages + ideas + where new posts are created, which then append to the global pool).
- **Generation Lite/PRO:** **Lite = single gpt-4o call** (cheap, default). **PRO** = the 6-step Конвеєр (per-step prompt+model), gated by `pro` flag (no real billing; topbar ⚡PRO pill + tariffs page toggle it). Studio «💡 Ідеї→пости» block (generate ideas → pick → posts). «👁 Промт» preview. regenerate/adapt keep ToV+deAI.
- **Images:** AI gen 3 providers (OpenAI/FLUX/Nano Banana, switch in Settings «🎨»), prompt comes free from Lite output; **text overlay** (sharp+SVG; 🧪 на беті - стилі: шрифт/місце/фон через `OverlayStyle`); onboarding auto-images; per-post editor = `openPhotoTool` (🧪 на беті двокроковий: джерело+кроп під формат → текст зі стилями; `openImageEditor` делегує туди ж). Media library + Google Drive (Picker) + upload-from-computer in composer; HEIC→JPEG; sharp thumbnails `/thumb/:name`.
- **Channels:** Telegram (shared bot `@R_Socialio_bot` — connect via deep-link/forward; or own bot), Instagram+Facebook (Meta OAuth), Threads. All post WITH image. `publishPostToChannels` shared by composer + autopost. No silent fallback.
- **Composer/publish:** `openComposer` = повноекранний 2-панельний overlay (ліворуч редактор: канали+рубрика+майстер-текст+dashed # хештеги / ⚡ AI фото (`openPhotoTool`, пропорції 1:1/4:5/16:9) / ✍ переписати; праворуч мобільне прев'ю на мережу + лічильники символів під ліміт). Кнопки: Зберегти чернетку · Підлаштувати під канали (з майстра) · дата/час · Запланувати · Опублікувати зараз. **Публікація один раз на мережу:** надіслані мережі заблоковані (✓), `publishPostToChannels` пропускає їх (`skipped`) і при ручній, і при плановій публікації. Calendar (Публікація): bank + drag-drop + AI-distribute + per-slot status (✅/⏳/⚠ + result tooltip), timezone-aware (workspace `timezone` setting); клік на слот → композер з `{slotId}`.
- **Analytics:** real, from `/api/published` (union of *_publish sent) — count + weekly chart + per-channel + «Останні публікації»; AI cost from llm_usage; **Instagram/Facebook card** (followers + IG reach-28d via `meta.igInsights`, needs `instagram_manage_insights`). Per-post engagement still deferred.
- **Gamification:** completeness score pill, tasks modal, nav badges, in-section task strip. **Tariffs** page (Lite vs PRO, fake billing).
- **Transcribers:** Fireflies (verified) / Grain / MeetGeek (best-effort, need real keys). Webhook auto-import for Fireflies.

## 7. IN PROGRESS / pending
- **✅ IMAGE EDITOR DONE** (per competitor study Predis.ai/Ocoya/PostNitro): `post.image_base` (base image w/o text) + `post.headline`; `images.ts` saves the base separately + `overlayForPost(ws,postId,headline,overlayOn)` (cheap re-overlay, no regen). Endpoints: `POST /posts/:id/image {headline}` (full gen), `POST /posts/:id/image-text {headline,overlay}` (cheap re-overlay on base), `/posts/:id/full` returns headline+has_base. UI: `openImageEditor(postId,onDone)` modal — preview, «Текст на зображенні» field, overlay toggle, «🎨 Згенерувати» (costs) + «✍ Оновити лише текст» (cheap, enabled once a base exists). Opened from the Studio card (click the image or 🎨 icon) and the composer («🎨 Зображення» button). Replaced the old tiny-icon-direct-gen (`genImageFor` removed).
- **Deferred (decided):** real billing/payment (PRO is flag-only); multi-account = **multi-workspace/brand switcher** (workspace_member table + active-ws in session); website-scan brand onboarding; engagement analytics; v1-step-3 pipeline cleanup (**SKIPPED — ~zero user value, gated PRO only; do not re-raise**).

## 8. Key decisions & gotchas
- **Secrets NEVER in chat/code/git.** Operator sets all keys in server `.env`. If user pastes a key in chat → tell them to REVOKE + add via `.env` themselves.
- **Classifier blocks** prod-DB writes via remote shell, secret-env dumps, and unauthorized `.env` edits → I verify via tsc / served markers / HEAD-match / health, NOT authed live tests (user clicks through the UI).
- Keep the product name **socialio/КонтентГров** (NOT "plyn" — that was just the design source). The current app.html IS the "Plyn" warm-paper light/dark design (commit 4132c5e).
- `defaults.ts` is intentionally EMPTY for brand fields (IG fills them) — old stale workspaces need «Почати з чистого листа» to refresh.
- Lite is the default path; the 6-step PRO pipeline is secondary/gated — don't over-invest in it.

## 9. Changelog (newest first — append every change)
- **🧪 НА БЕТІ: 🎨 ФОРМАТ як повноцінний вимір планування (Фаза A з розбору «пости/каруселі/рілси/сторіс»).** Формат ортогональний рубриці: рубрика = ПРО ЩО, формат = ЯК УПАКОВАНО, канал = КУДИ - так це в усіх професійних календарях (в Airtable це готове поле `Content Type`, у шаблоні Later окремі колонки `content type` і `content pillars`). **Свідомо БЕЗ окремих розділів/вкладок на формат** - це порізало б стрічку на чотири й зламало модель єдиного пулу Студії; формат живе як бейдж+фільтр+перемикач, як зараз живе рубрика. ① `plan_slot.format` (alter) - раніше план узагалі не міг сказати «цей слот - карусель»; `post.format` розширено 'post'|'carousel'|'reel'|'story'. ② **Мікс форматів ПО МЕРЕЖАХ** у вже наявному `channel_rhythm` (реюз спадкування «як у бренду»): чекбокс+частка% на мережу. Мікс саме per-network - це доказова частина (LinkedIn виграє документами-каруселями 7% ER, IG рілсами по охвату, ті самі IG-каруселі дають 4.7× переглядів проти TikTok). **Дефолт - лише пости**, тож поведінка не змінюється, поки юзер не налаштує мікс явно; частки підписані як «орієнтир, не точна математика» - усі цифри типу «60/25/10», що гуляють по блогах, це фольклор без методології (джерела розходяться на 20 п.п.). ③ **Формат керує ГЕНЕРАЦІЄЮ, не лише мітками:** `FORMAT_RULES` у `buildLitePrompt` (обидві гілки) - карусель пишеться слайдами «Слайд N:», рілс сценарієм ХУК/БІТ/CTA, сторіс кадрами. Структурне правило **«слайд 2 = ДРУГА обкладинка»** - єдине тут із реальним доказом: Мосері підтвердив, що Instagram пересервує саме слайд 2 тим, хто не свайпнув (Metricool міряє +53.57% переглядів каруселей р/р). ④ **Формат, який Розвідник УЖЕ радив, більше не губиться:** `extractIdeasFromText` віддає `fmt` (внутрішній ключ поруч із укр. міткою для бейджа) → доїжджає через `/materials/:id/posts {formats}` до `post.format`; раніше модель його рахувала, UI показував бейджем, а при генерації він просто відкидався. ⑤ Формат у темах скелета (`buildLiteSkeleton` дає моделі підказку, що тема каруселі ≠ тема рілса), слот→пост передає формат, селектор «Формат» у композері, бейдж у плані й іконка на привидах-темах календаря. ⑥ **📊 Фактичний мікс за 30 днів** (`/api/plan` += `realizedMix`, смужка під ритмом каналів) - цього не показує ЖОДЕН конкурент: у Buffer/Later формат живе або лише в аналітиці, або в ручних тегах (їхні ж довідки визнають, що юзери ліплять теги «для content type»), а календарний фільтр за форматом нативно є лише в Metricool. Нейменг «Формат» (не «тип поста») - бо «post type» у Publer означає стан життєвого циклу, колізія реальна. build 0, test 17/17, eslint 0, smoke 27→29 (+formatDim/formatPlan). **Далі (Фаза B, окремо):** карусель end-to-end - `post.slides`, рендер слайдів через уже наявний `overlayHeadline` зі спільним стилем, публікація IG (children→CAROUSEL), LinkedIn (PDF-документ), Telegram (sendMediaGroup); Threads-карусель через API ще НЕ перевірена. Сторіс свідомо відкладено: за дослідженням це шар каденсу, а не частка плану стрічки.
- **🧪 НА БЕТІ: 🛠 Технічний аудит, Рівень 1 (надійність публікації).** ① **Захист від подвійної публікації НА РІВНІ БД:** unique-індекси на `telegram_publish(post_id,target)` / `threads_publish(post_id)` / `meta_publish(post_id,channel)` / `linkedin_publish(post_id)` (з самолікуючим дедуп-DELETE перед створенням - безпечно для старих дублів, якщо лишились). `publisher.ts` тепер РЕЗЕРВУЄ рядок (`INSERT...ON CONFLICT DO NOTHING`, status='sending') ПЕРЕД зовнішнім викликом мережі, а не пише його вже ПІСЛЯ успіху - раніше дедуп був лише SELECT-потім-INSERT у коді, і гонка (подвійний клік «Опублікувати», збіг ручної публікації з автопостом) теоретично могла проскочити повз нього. Збій публікації звільняє резервацію (DELETE) для повторної спроби. ② **Сторож зависаючих слотів:** нова `schedule_slot.updated_at` (alter) + автопост на кожному тіку скидає `status='posting'` назад у `'planned'`, якщо минуло 15+ хв - раніше слот, застряглий у 'posting' через крах процесу (деплой/OOM) посеред публікації, випадав НАЗАВЖДИ і з автопосту, і з `/api/schedule/auto` (яке явно виключає posting/posted), без жодної помилки в UI. ③ **Meta (Facebook/Instagram) token expiry:** `meta_config.token_expires_at` (уже існував у схемі, але ніколи не читався) тепер перевіряється ПЕРЕД публікацією - дружнє «Токен Meta протух - перепідключи» замість сирої помилки Graph API, як уже було зроблено для LinkedIn. **Свідомо НЕ зроблено зараз** (Рівень 2/3 з аудиту, окремі сесії): паралелізація публікації по мережах, DB-інтеграційні тести на дедуп-логіку (потребують тестової БД - більша інвестиція, ніж «швидкий фікс»), розріз `server.ts`. build 0, test 17/17, eslint 0, smoke 27/27 (без UI-змін - suite незмінний).
- **🧪 НА БЕТІ: 🎯 Батч фіксів по СММ-аудиту (9 пунктів, рішення Олега по кожному).** ① **`max_tokens` масштабується від к-сті постів** (`openrouter.ts` `ctx.maxTokens`, дефолт 1500 лишився для решти 20+ викликів) - раніше генерація 8-12 постів одним JSON-масивом впиралась у дефолтний ліміт: пости мовчки обрізались АБО модель стискала кожен через брак місця, а не бо так краще; тепер `Math.min(8000, 700+n*500)`. ② Прибрано «само-критику в тому ж проході» (була театром без реальної перевірки) - замінено на реальні опційні ворота якості (п.8). ③ **Конфлікт двох гачок-правил вирішено пріоритетом:** якщо матеріал дає сильний факт/цифру - гачок з кульмінації (HOOK_RULE), і лише якщо НЕ дає - відкриття болем клієнта (painThesis PROBLEM-MATCH); було - обидва правила одночасно, модель вирішувала навмання. ④ **Фіксоване меню з 6 типів гачків (причина «однакових постів у всіх брендів») замінено на навчання з ВЛАСНИХ voice_examples** - модель вивчає, ЯК САМЕ автор відкриває пости, і повторює його манеру; старе меню лишилось лише фолбеком, коли прикладів голосу ще нема. ⑤ **`presence_penalty:0.3`** додано глобально в `chat()` - дешевий і надійніший важіль проти шаблонних фраз, ніж лише розширення regex-каталогу AI_TRACE_RX. ⑥ **LinkedIn - повний плейбук** (dwell time, коментарі в перші 60-90хв, «…more»-ліміт, без корпоративного жаргону) доданий у `CHANNEL_PLAYBOOK`; одним записом каскадно чинить 3 місця - `adaptForChannels` (був однорядковий фолбек), `generateChannelPlan` (кидав «канал не підтримується»), `atomizePost` (LinkedIn мовчки виключався з розтиражування). ⑦ **Памʼять між постами:** нова `recentContentDigest(ws)` - звичайний SQL-запит по вже наявних publish-таблицях (10-15 останніх опублікованих постів) + один дешевий виклик-стиснення в короткий список «вже використаних гачків/цифр/CTA», інжектиться і в генерацію постів (`buildLitePrompt` v2), і в теми контент-плану (`buildLiteSkeleton` - фікс «план і стратегія не метчаться» від повторів тем). **Свідомо БЕЗ векторної БД чи RAG** (обговорювали alibaba/zvec) - обсяг замалий для ANN-інфраструктури нового сервісу; якщо колись знадобиться семантичний пошук по тисячах постів, природний наступний крок - `pgvector` як розширення вже наявного Postgres, не окремий сервіс. ⑧ **Опційні ворота якості:** нова панель «🛡 Автоперевірка якості» (Бренд→Голос, 3 чекбокси - Директор/AI-сліди/Сторителлінг, `settings_block.qa_gates`, дефолт вимкнено); увімкнені перевірки прогоняються АВТОМАТИЧНО після генерації (`runQaGates` у `generatePostsOnePass`, паралельно по нових постах), компактний підсумок пише `post.qa` (alter, jsonb); Студія показує бейдж лише коли перевірка щось знайшла - клік відкриває той самий live-модал, що й раніше (нічого не переписується мовчки). ⑨ Прибрано «само-критику» з `<task>` (див. п.2) - реальну перевірку тепер дають опційні ворота, дубльований невиконуваний інструмент не потрібен. **Свідомо НЕ зроблено зараз:** повний перехід на двопрохідну генерацію (чернетка→точкова правка) - п.④+⑨ вже суттєво зменшили щільність промпту без подвоєння вартості основної генерації; лишається ідеєю на майбутнє, не пріоритетом. build 0, test 17/17, eslint 0, smoke 26→27 (+qaGates).
- **🧪 НА БЕТІ: 📖 Сторителлінг-редактор (адаптація скіла storytelling-corrector Г.Рівери).** Новий пункт «📖 Сторителлінг» у ⋯-меню картки (група «Покращити», поруч із Директором): `storytellingVerdict(ws,content)` оцінює пост як історію за 12 прийомами (Хук, К-А-Р-В, «але/тому» замість «і потім», підняті питання, сцена, сирі думки, діалог, емоція через тіло, ритм, тон+післясмак, ставки, злам очікування) - json-режим, `env.cheapModel`, той самий діагностичний патерн, що й Директор/AI-сліди. Модалка: оцінка X/10 + вердикт + до 5 правок «Було→Пропоную→Чому» (точна цитата → переписаний варіант → 1 фраза чому) + компактний чек-лист усіх прийомів (✅/⚠️/❌/—). Нічого не переписується мовчки: юзер бачить правки і сам тисне «✍ Застосувати» - тоді всі suggest-варіанти йдуть ОДНИМ instruction у вже наявний `/regenerate` (rewritePost, той самий рушій, що й у Директора, з повним брендовим контекстом і НЕ вигадує факти за автора). Business-модуль скіла (кейси/офери) свідомо не портовано в v1 - "подивимось, що вийде" (рішення Олега). build 0, test 17/17, eslint 0, smoke 25→26 (+storytelling).
- **🧪 НА БЕТІ: 🩹 Фікс «кожен пост починається з питання» (фідбек Олега з реального DM-поста в боті).** Корінь - `OBJECTION_RULE` (pipeline.ts) давав моделі приклади заперечень у формі готових питань («чому цьому вірити?», «а в моєму випадку спрацює?») як інструкцію «подумки визнач і зніми фактом», але gpt-4o регулярно «протікав» саме ці питання в перший рядок поста замість мовчазного знаття. Правило переписано явно: заперечення визначати **НЕ в тексті**, і **саме питання чи його майже дослівний переказ виводити в готовий текст ЗАБОРОНЕНО** - лише готова відповідь фактом/цифрою/прикладом. Питання-гачки як такі НЕ заборонені (HOOK_RULE і далі дозволяє «цікавісний розрив» як один із типів) - прибрано конкретно цей витік із OBJECTION_RULE. Одна константа, впливає на всі 5 точок генерації (Lite v2+legacy, adapt, rewrite, extractIdeas). build 0, test 17/17.
- **🧪 НА БЕТІ: 🔀 Воркфлоу-лійка + 📡 канали публікації на «Сьогодні» (Олег підгледів у конкурентів) + фікс 500 на тейках Threads.** ① **Лійка** (Новини→Чернетки→Опубліковано, клікабельно): `/api/today` += `netToday` (скільки пішло СЬОГОДНІ по кожній мережі окремо: telegram/threads/instagram/facebook/linkedin, один union-all запит) + `publishedToday` (сума); UI - 3 картки-лійка над «Швидким стартом» замінили 2 дублюючі тайли («Нові матеріали»/«Чернеток усього»). ② **Панель каналів:** 5 карток (іконка+назва мережі, реюз `NETICON`/`NETVAR` з існуючих `chanDots`/`sentDots`) - підключені показують «✓ підключено» + N постів сьогодні, непідключені - «Підключити →» (клік = Налаштування→Канали); дані з уже наявного `/api/channels/status`, нового запиту не знадобилось. ③ **Фікс `⚠ 500: порожня відповідь моделі` на «🧵 тейках»:** корінь - без примусового JSON-режиму gpt-4o інколи ігнорує «поверни лише JSON-масив» і відповідає прозою (уточнення/відмова), особливо коли нема палива (порожній Банк ідей і щоденник) - `extractJsonArray` на такому тексті мовчки повертав `[]`. Фікс: `chat()` += опційний `ctx.json` (response_format:json_object, лише коли явно проситься - інші виклики не зачеплені) + `generateThreadsTakes` тепер просить `{"takes":[...]}` у JSON-режимі з одним ретраєм при порожньому результаті. build 0, test 17/17, eslint 0, smoke 24→25 (+todayFunnel).
- **🧪 НА БЕТІ: 🦉 Сова v2 (фідбек Олега - позиція+поведінка) + фікс застарілих підказок.** ① **Гніздо ліворуч-внизу:** дефолтна позиція сови тепер фіксована точка bottom-left (було bottom-right); до цілі підказки вона КОРОТКО ЛЕТИТЬ (розправлені крила, змах `owlFlapL/R`, боб) і одразу повертається назад - не «телепортується» і не сидить постійно біля кнопок. ② **Перетягування:** `owlEnableDrag` (pointer-події, поріг 6px відрізняє драг від кліку) - сову можна відсунути, якщо заважає; вона лишається на новому місці до наступного ховання бульбашки, а тоді самостійно летить назад у гніздо (`owlGoHome`). Бульбашка авто-фліпає бік (`.flip`), щоб не вилазити за край, коли сова близько до правого краю екрана. ③ **Прибрано «клік → миттєво вимикається» (фідбек Олега):** кнопку бульбашки «не показувати» видалено - закриття (✕) тепер ЗАВЖДИ лише ховає бульбашку й летить у гніздо, не чіпаючи сама асистента; єдиний реальний перемикач увімк/вимк переїхав у меню аватара (`🦉 Помічник Розум: увімк./вимк.`, стан видно одразу). ④ **Фікс застарілих підказок** («3 пости без фото» після того, як фото вже додано): `loadGuide(true)` - тихий фоновий рефреш стану з сервера БЕЗ зайвого вильоту сови; якщо бульбашка зараз відкрита і показана підказка вже нерелевантна - тихо перемикається на актуальну або ховається. Підключено після затвердження поста (композер/Сьогодні/масові дії) і після збереження фото у фото-інструменті (`#ptDone`) - ключові дії, що впливають на підказки. build 0, test 17/17, eslint 0, smoke 22→24 (+owlHome/owlDrag).
- **🧪 НА БЕТІ: 🦉 Розум - сова-провідник (помічник #8 з батчу Олега).** Анімований векторний маскот (SVG, бренд-помаранч #f2a007 + чорний, окуляри знімні, кліпає, сидить на «R», інколи жартує/підстрибує; `prefers-reduced-motion` вимикає анімацію). **Двигун наступного кроку:** `GET /api/guide/next` рахує стан воркспейсу (канали/бренд/болі/план/джерело/чернетки-на-затвердження/затверджені-без-календаря/без-фото/Threads-без-поста-сьогодні) і віддає ПРІОРИТЕЗОВАНІ підказки, кожна з ціллю (CSS-селектор кнопки, куди летіти) і дією (навігація або do:takes/addmaterial). Сова летить до потрібного контролу, показує репліку + кнопку-дію (+«далі ›», «не показувати»). Після дії - перерахунок кроку. **Навчання:** `guide_log` (shown/clicked/dismissed/off) - основа під персоналізацію. `guide_off` у settings; ре-увімкнення - пункт «🦉 Помічник Розум» в меню аватара. `plural()` для укр. числівників. build 0, test 17/17, eslint 0, smoke 21→22 (+owlGuide). Наступні ітерації (за бажанням): вибір персонажа, більше емоцій/сценок, LLM-порада під конкретний контекст.
- **🧪 НА БЕТІ: 🧩 Батч фідбеку Олега (8 пунктів, №8-помічник - окремо).** ① **Єдиніший ряд вкладок Створення:** активна вкладка тепер яскрава (brand-колір, білий текст), дубль-заголовок «Чорновики» під табами прибрано (зливався з вкладкою). ② **📔 Щоденник:** КОЖЕН запис = ОКРЕМЕ джерело `📔 Щоденник, D · HH:MM` (не зливаються в «кашу»); довгий (>400 симв) фоново розкладається на теми (`splitDiaryTopics`). ③ **🎬 Рілси:** тривалість керується СЛОВАМИ озвучки (60с ≈ 115-140 слів - фікс 17-секундних; біт 20-28 слів); промт переписано на ІСТОРІЮ (бан гасел/абстракцій, хук із цифрою, причинні біти). ④ **Threads-налаштування переписано зрозуміло** (📮 «Заклик окремою відповіддю» + 💬 «Щоденна порція тейків», кожне з поясненням що/коли робить); логіку перевірено - CTA-джоба (`threads_reply_job`) і щоденні тейки (`threads-auto`) реально працюють. ⑤ **Зображення не засмічують галерею:** при підміні фото старе ПОХІДНЕ медіа (ai/crop/pexels/ai-base) видаляється, якщо ним не користується інший пост (`cleanupDerivedMedia` у 3 точках); базові `ai-base` сховані з медіатеки. ⑥ **Формат зображення зрозуміліший:** дефолт 4:5 (найбільше місця в стрічці, валідний усюди), підписи «📱 Для стрічки / ⬛ Квадрат / 🖥 Широке» + пояснення «один формат на всі мережі». ⑦ **Матеріали - розумна ретенція:** ліміт 60→200, щоденники завжди зверху й НЕ видаляються; нічна ретенція `sweepStaleNews` (раз/добу архівує вчорашні новини з оцінкою <6, дешевий Gemini; соц/діарі не чіпає); топ-матеріал (⭐≥9/10) → сповіщення в Telegram із кнопкою «✨ Зробити чорновик» (`notifyTopMaterials` + callback `mat_post:`). build 0, test 17/17, eslint 0, smoke 21/21.
- **🌿 У ГІЛЦІ (ще не на беті): 📔 Щоденник по-новому + 🎬 рілси-історії (пункти 2-3 фідбеку Олега; батч перервано - решта пунктів чекає рішення).** ① **Щоденник:** КОЖЕН запис (текст/голос/фото) = ОКРЕМЕ джерело `📔 Щоденник, D місяця · HH:MM` (більше не зливаються в одну «кашу» дня); довгий запис (>400 симв) фоново розкладається на окремі теми (`splitDiaryTopics`, cheapModel, дослівні фрагменти автора: перша тема займає місце оригіналу, решта - нові матеріали; лог «розкладено на N тем»). `ensureDiary` прибрано. ② **Рілси:** `reelLenRule` += цільові СЛОВА озвучки (60с ≈ 115-140 слів, біт = 20-28 слів; 17-секундна проблема = гасла по 4 слова); промт `reelsScript` переписано: ІСТОРІЯ замість реклами (конкретний випадок/поворот з матеріалу, біти зʼєднані причинно, бан гасел і абстракцій «інновації/прозорі процеси/приєднуйся», хук з цифрою з самої історії, самоперевірка к-сті слів). build 0, test 17/17.
- **🧪 НА БЕТІ: 🧘 Батч «легше і чистіше» (3 пункти фідбеку Олега).** ① **Рейку Студії прибрано** (зʼїдала 320px заради однієї кнопки): Чорновики на всю ширину; тейки = компактна двокнопка в шапці Чорновиків - «🧵 5 тейків» (tooltip з поясненням) + «⚙» (одразу в Налаштування → Канали, до стратегії Threads). ② **Опубліковані авто-ховаються з Чорновиків:** дефолтна вкладка тепер «Активні» (= без опублікованих; лічильники чесні), опубліковані живуть ТІЛЬКИ у вкладці «✈️ Опубліковані» - шум зник, видаляти їх не треба (вони - історія публікацій і аналітика). ③ **«Сервіс виглядає важким» - прогресивне розкриття:** 7 просунутих панелей (🧬 ДНК, 🪪 Паспорт голосу, 🧲 Магніти, 🪜 Сходи офферів, 📮 CTA, 📏 Формат, 🎥 b-roll) за замовчуванням згорнуті в один рядок-заголовок «… налаштувати ▾» (клік розгортає; `.panel.folded`+`.pfh`, boot-IIFE за emoji-якорями .ph). Дані/обробники не чіпаються. smoke 19→21 (+studioClean/foldedPanels). build 0, test 17/17, eslint 0.
- **🧪 НА БЕТІ: 🎓 Навчальні задачі (гейміфікація) оновлено під актуальний продукт:** /api/tasks 14→18 задач за реальним воркфлоу (бренд → стратегія → канали → джерела → створення → публікація): + `pains` (болі+теза - рушій генерації, 10), + `goal` (ціль, 5), + `plan` (контент-план, 10), + `bot` (бот-асистент tg_owner, 10), `chanAll`→`chan2` («другий канал», CORE-модель замість «всі канали»; враховує LinkedIn), транскрибатор/GDrive підписані «Інструменти:». TASK_TARGET += pains/goal/plan/bot; порядок груп модалки = воркфлоу.
- **🧪 НА БЕТІ: 🧹 `sweepThinMaterials` (разова зачистка при старті поллера, ВСІ воркспейси):** старі «голі» матеріали (заголовок+«Джерело:»+лінк, «мʼясо» < 120 симв після зрізання URL-ів) архівуються (дедуп живий, пости не чіпаються); соцджерела виключені (cs.kind/url-маркери rsshub|telegram|threads). Лог «заархівовано N». Ідемпотентно (після 1-го прогону кандидатів ~0).
- **🧪 НА БЕТІ: 📥 Джерела - тільки повний контент (фікс «заголовок+лінк») + ⚡ «Сьогодні» v2.** ① **Ремонт тягнення статей (фідбек Олега «тягнеться заголовок і посилання - ремонтуй»):** `extractParagraphs` (rss.ts, тепер export) значно сильніший - **JSON-LD articleBody** (чимало видавців кладуть ПОВНИЙ текст у NewsArticle-схему - працює навіть де розмітка закрита) → `<article>`-скоуп для абзаців (відсікає сайдбари/тизери) → og:description + meta description фолбек; юніти 14→17 (+3 на екстрактор). ② **Гейт якості:** якщо стаття НЕ витягнулась (Cloudflare/JS-only) чи тіло < 220 симв - айтем у Матеріали НЕ додається; пишеться тумбстоун (source archived=true, `[стаття недоступна] url`) щоб дедуп не пробував щотіка; лог «пропущено N без тіла». Соцджерела (Telegram/Threads RSSHub, IG) НЕ гейтяться - короткий пост там легітимний (`isSocial` за url/kind). ③ **«Сьогодні» v2** (аналіз: екран не показував ЗБОЇ і не замикав цикл): `GET /api/today` += `failed[]` (слоти status='failed' за 48г) → червона панель «⚠ Не опублікувалось» з причиною і кнопкою «Відкрити й повторити» (композер зі слотом); += `freshMaterials {count,top}` (24г, без тумбстоунів) → тайл «📥 Нові матеріали · топ ⭐N/10» (клік = Матеріали); += `publishedYesterday` (union 4 publish-таблиць за таймзоною) → тайл «✈️ Вчора вийшло» (клік = Аналітика). build 0, test 17/17, eslint 0, smoke 18→19 (+todayFails).
- **🧪 НА БЕТІ: 🎯 Стратегічний батч 2 - тег-намір + CTA-політика + ціль глибше в генерацію.** ① **`post.intent`** (alter: awareness/nurture/sale): Lite-контракт JSON += поле intent з правилом міксу (більшість awareness, sale ≤ ~1 із 5; ціль «гроші/ліди» → 1 із 4), парсинг з валідацією (фолбек awareness), пишеться при генерації. ② **CTA-політика за наміром** в `adaptForChannels(+intent?)`: awareness = НУЛЬ продажних закликів (цінність/питання), nurture = лише мʼяка механіка (кодове слово/лід-магніт), sale = повний CTA за cta_config; intent передається з /adapt-роуту і publisher-автоадаптації (селекти += p.intent). ③ **Ціль у скелеті плану**: `buildLiteSkeleton` темам += `goalRule(s)` - теми плану тепер тягнуться до primary_goal+метрики. ④ **UI:** чіп наміру на картці Студії (🌱 знайомство / 🤝 прогрів / 💰 продаж, з tooltip), селект «Намір» у композері поруч із рубрикою (PUT /posts/:id приймає intent; saveDraft шле). Смоук 17→18 (+intentChip; cmpBack тепер перевіряє і cmpIntent). build 0, test 14/14, eslint 0, smoke 18/18. (4 типи контенту - пропущено за рішенням Олега.)
- **🧪 НА БЕТІ: 🤖 Власний бот = повний асистент + 📐 стратегічний батч 1 (CORE-дефолт плану + болі-рушій генерації).** ① **Власний Telegram-бот з UI (запит Олега для бета-тесту):** токен у Налаштування → Канали → Розширені тепер вмикає НЕ лише публікацію: `registerOwnBotWebhook` (tgbot.ts) реєструє боту вебхук `?bot=<id>` → роут `/api/webhooks/telegram/:secret` роутить апдейти власним токеном (пошук за префіксом id, токени TG = `<botId>:<hash>`); `handleUpdate/handleCallback(tokenOverride)`, `attachChannel(token)` (getChatMember своїм botId, конфіг пише свій токен), `createConnectLink` дає deep-link на СВОГО бота, `wsBotToken(ws)` + `liveSend` через нього → щоденник/дайджест/ідеї/кнопки їдуть з власного бота (двигун digest/diary шле все через liveSend - покрито автоматично). UI tgSave показує «✅ @бот підключено повністю». На беті: вставити токен у налаштуваннях кабінету - жодного .env. ② **План: дефолт повернуто до CORE** (стандарт індустрії, 9 джерел): ОДИН майстер-план ('all'), чекбокси мереж сховані за тумблером «🔬 Окремий план на кожну мережу» (localStorage kg_plan_adv; дефолт OFF; агенційний режим лишився опцією); підпис генерації «майстер-план (адаптація при публікації)». ③ **Мікс рубрик видимий:** `planMixWrap` - кольоровий бар часток рубрик зі Стратегії + легенда (план явно будується за пропорцією). ④ **Привиди-теми в календарі** з іконкою мережі (per-network слоти). ⑤ **🧭 «Позиціонування і болі клієнта»** (панель у Стратегії→Бренд): settings `brand_thesis` («Х для Y») + `pain_points` (рядки «біль → рішення → доказ»; «✨ Підказати» = `POST /api/brand/suggest-pains`, cheapModel з брифу); `painThesis(s)` (pipeline.ts) інʼєктується в Lite v2+legacy, extractIdeas, rewritePost: **PROBLEM-MATCH правило** (гачок = біль словами клієнта, PAS-агітація, «лиходій» = стара система, пост самодостатній, прямий продаж ≤ кожного ~5-го поста). build 0, test 14/14, eslint 0, smoke 15→17 (+planDefault/painsPanel).
- **🧪 НА БЕТІ: 🎯 Фідбек Олега (бе-тест) - напрям генерації + план по мережах + розумний AI-розподіл.** ① **«Про що писати» (топ-запит, 3× у фідбеку):** нове `POST /api/generate/topic {topic,count,channels?}` (source origin='topic' → `generatePostsOnePass` з кутами; опційно націлює пости на мережі) - у `openAddMaterial` тепер 2 вкладки: **✍️ Написати про тему** (текст напряму + 1/3/5 постів → одразу в Чорновики) і 📥 Додати матеріал (стара сировина). Плюс топік-поле «Про що зробити план?» у генераторі плану (сідиться в `buildLiteSkeleton(opts.topic)` - вписує задані теми в скелет, решту доповнює за рубриками). ② **План ПО МЕРЕЖАХ (рішення Олега «окремий план на кожну мережу»):** `POST /api/plan/generate` перероблено на `{networks[],horizon,posts_per_week,topic}` - для КОЖНОЇ обраної мережі свій скелет (`buildLiteSkeleton(opts.network)` з тон-хінтом під платформу, channel=<мережа>); `buildLiteSkeleton` += `opts{topic,network}`. UI: чекбокси мереж `#planNets` (з підключених каналів), топік-поле, «≈N постів (per×мереж)»; вкладки плану `planChanTabs` тепер за РЕАЛЬНИМИ мережами плану (Усі + по мережі з лічильником), не за PRO. Слот→пост уже націлював `channels` на мережу слота (гард на 'all'). **Прибрано баг «Threads-скелет пише Telegram / 0 пунктів / немає плану»** - стара PRO-гілка `generateChannelPlan` (флапала []) більше не в генерації плану (ендпоінт лишився дормантним). ③ **Розумний AI-розподіл** (`/schedule/auto` крок 4): був стос - `times.length` постів/день на вузьких best_days (6 постів в один день). Тепер РІВНОМІРНО: спершу по 1 посту на кожен дозволений день горизонту, і лише коли днів забракло - друге коло (2-й/день з іншим часом). 9 постів → 9 різних днів. ④ **Кнопка «← Назад» у композері** (зліва в шапці, зрозуміліше за кутовий ✕; планування вже закривало з тостом). build 0, test 14/14, eslint 0, smoke 12→15 (+planNets/topicMode/cmpBack).
- **🧪 НА БЕТІ: 🚀 Батч зручності (швидкий старт + текстовий онбординг + повний розділ «Інструменти»).** ① **Швидкий старт у «Сьогодні»:** `GET /api/today` += `quickstart {channels,plan,approved}` (три `count`-запити: підключені канали TG/Threads/Meta/LinkedIn, наявність `plan_slot`, наявність `review='approved'` поста). `loadToday` малює панель «🚀 Швидкий старт» ЗВЕРХУ, поки хоч один із 3 кроків не виконано (підключи канал → згенеруй план → затверди перший пост); кожен рядок = чек ✓ / номер + кнопка-перехід (`settings→channels` / `publish→plan` / `create→posts`); зникає, коли всі три готові. ② **Онбординг без Instagram:** на кроці 0 (`renderObConnect`) під кнопкою IG зʼявилась «✍️ Почати без Instagram - розповім про бренд текстом» → `obIdx=1` (кроки 1-2 вже збирають маркетинг-контекст (req) + приклади голосу текстом, бекенд не змінювався; голос виводиться з `voice_examples`). ③ **Розділ «🧰 Інструменти»** (нова `<section data-view="tools">`): панелі **Google Drive** і **Транскрибація (Fireflies)** ФІЗИЧНО переїхали з Джерел через host-move (`toolsGdriveHost`/`toolsTransHost`, вузли ті самі - обробники живі; grid2 з Медіа-бібліотекою лишається на всю ширину); + кнопки «🧪 Конвеєр генерації (PRO)» і «👁 Промт генерації» (переїхав із заголовка Чорновиків). `umTools` тепер веде в `selectView('tools')` (не одразу в конвеєр); задачі гейміфікації transcriber/gdrive ведуть у tools. ④ **⋯-меню картки спрощено:** 4 часті дії одразу (↻ Переробити · 🎨 Зображення · 🔥 Продовження · ⧉ Копіювати) + «🧰 Більше інструментів» (складений блок: Директор, Магніт, Threads повтор/розгортання, COPE, Рілс) - розкривається на місці. **Смоук** (`ui-smoke.mjs`) розширено 8→12: +quickstart (панель + «1 з 3» + 2 кнопки), +toolsView (панелі переселено в хости, кнопки на місці), +obNoIg (альт-кнопка рендериться), +cardMenu (4 часті + складений «Більше»). build 0, test 14/14, eslint 0, smoke 12/12.
- **🧪 НА БЕТІ: ⚡ Екран «Сьогодні» + чистка навігації (UX-план ①-④ з аудиту).** ① **«Сьогодні»** - перший розділ (десктоп+мобайл, дефолт при вході): `GET /api/today` (слоти дня за таймзоною, топ-3 чернетки + draftsTotal, ідеї, найближча тема плану, стрік Threads) - тайли (стрік/💬 коменти async ?countOnly=1/ідеї/чернетки, клікабельні), «📤 Сьогодні виходить» (клік = композер зі слотом), «✅ На затвердження» (✅/✍ прямо там), «⚡ Зробити зараз» (✍️ пост із теми дня + 🧵 3 тейки). ② **Переїзди:** реплай-коуч = `openThreadsComments()` - виклик з тайла «Сьогодні» і кнопки «💬 Коменти» в Threads-панелі Аналітики (з Налаштувань кнопку прибрано); Банк ідей = вкладка «💡 Ідеї» у Створенні (layIdeas, loadIdeasTab; чіп у Матеріалах прибрано; дії банку освіжають обидва місця через refreshIdeaViews). ③ **Бренд+Стратегія злиті:** розділ «Бренд і стратегія», вкладка «🎯 Бриф і цілі» (brandStratHost; вміст секції strategy фізично переїздить при старті, вузли живі); selectView('strategy') - шим на brand+setBTab('strat'); навігація 5 пунктів: Сьогодні·Створення·Публікація·Бренд·Аналітика. ④ **Конвеєр → «🧰 Інструменти»** в аватар-меню (layoutTabs сховано назавжди; не-PRO бачить тост); «Контент-план по каналах» (LLM-таблиця) прибрано з UI (код дормантний). **Браузерний смоук** (playwright-core + стаб API, `server/.lint/ui-smoke.mjs`): 8/8 (todayActive/slot/draft/streak/commCount/ideasTab/stratShim/toolsItem), 0 pageerror.
- **🧪 НА БЕТІ: 🏗 Рефакторинг-батч (аудит → фази 1-5).** ① **Тести в repo:** `server/test/*.test.mjs` (node:test, без залежностей; 14 юнітів: `splitTextForThread` (новий чистий експорт фолбека гілки), `charFrac/textPx/wrapPx` (експортовані з images), `cleanText/parseFeed/parseFeedTitle`, `scanAiTraces`) - `npm test` (глоб `test/*.test.mjs`; `node --test test/` з diretórios ЛОМИТЬСЯ в цьому node). ② **ESLint** (devDeps eslint+globals; `eslint.config.mjs`): public/app.js + test - правила лише баг-класи (no-use-before-define {functions:false} = ловить TDZ типу «📣 не працює», no-undef (globals: browser+gapi+google), no-redeclare, no-dupe-*); знахідки виправлено: глобальний стан app зібрано в один блок угорі (curLayout/PRO/Rubrics/ChanStatus/ThStrat/Pub/NETS/CP_LABEL/obVoiceImported), `close`/`saveDraft` у композері → function-декларації. ③ **CI:** `.github/workflows/ci.yml` (push у main/beta/claude/**): npm ci → build → test → lint - сигнал, деплой не блокує. ④ **app.js:** ВЕСЬ JS (2300 рядків) винесено з app.html у `public/app.js` (`<script src="/app.js">`; той самий глобальний скоуп; лінтиться напряму; наступний крок - різати на composer/studio/calendar/settings). ⑤ **Хелпери:** `src/settings.ts` (`getSetting<T>/setSetting/getSettingText` - одна точка JSON-парсингу settings_block; впроваджено в publisher+threads-auto); publisher `freshToken()` (спільний refresh-патерн YouTube/TikTok); logEvent warn у тихий збій авто-адаптації (раніше Threads>500 симв. падав «незрозуміло чому»). ⑥ server.ts: ЗМІСТ-банер угорі + **TODO: фізичний розріз на src/routes/* - ОКРЕМА сесія, по одному модулю з деплоєм** (інтеграції перемішані з post-роутами, механічний зріз небезпечний).
- **🧪 НА БЕТІ: 💬 П7 Реплай-коуч (Олег увімкнув threads_manage_replies).** OAuth-скоуп += threads_manage_replies (⚠ у токен потрапляє лише при ПЕРЕпідключенні Threads - Олегу перепідключити в Налаштування → Канали); `threads.mediaReplies(mediaId)` (id,text,username,timestamp); `suggestThreadReplies(ws,items)` - один gpt-4o виклик на всі коменти, ≤200 симв, розмовно, зустрічне питання; `GET /api/threads/comments` (6 останніх постів за 7д → коменти ІНШИХ людей, дедуп відповіданих через settings `th_replied` (кеп 200), людська підказка про перепідключення при permission-помилці) + `POST /api/threads/reply {commentId,text}` (publish reply_to_id=комент). UI: кнопка «💬 Коменти» у Стратегії Threads → модалка: комент + редагований AI-драфт + «↩ Відповісти» (після відправки картка тьмяніє). Мосері-принцип «відповідай більше, ніж постиш» тепер робиться за 2 хв на день. П1-П8 Threads-треку ЗАКРИТО повністю.
- **🧪 НА БЕТІ: 🛠 Надійність публікації IG/Threads + оверлей 4:5 + дрібниці з фідбеку.** ① **IG «Media ID is not available» / Threads «The requested resource does not exist» - КОРІНЬ:** обидва API обробляють медіа-контейнер АСИНХРОННО (тягнуть фото з нашого /media), а ми кликали media_publish/threads_publish одразу після create. Тепер: `meta.publishToInstagram` і `threads.publish` полять статус контейнера до FINISHED (фото 1-3с, до 40с) + ретраї публішу з бекофом саме на ці помилки (4 спроби). Гілка Threads: root фіксується sent ОДРАЗУ після публікації (повтор не задублює root), збій ветки не валить публікацію (logEvent + break), пауза між ветками 1.5с→3с. ② **Оверлей вилазив за край на 4:5:** wrapWords рахував ширину символа константою 0.6×fs, а CAPS-кирилиця bold ≈0.8×fs → `charFrac` (класи: вузькі/ВЕЛИКІ 0.8/найширші МШЩЮЖФ 0.98/рядкові 0.66) + перенос по ПІКСЕЛЯХ (`wrapPx`) + shrink-to-fit (слово ширше поля → шрифт зменшується до 6 ітерацій); plate-ширина з реальної ширини рядка. Перевірено рендером: кейс зі скріна Олега + «НАЙВІДПОВІДАЛЬНІШИЙ». ③ Композер: «✅ Затвердити» тепер закриває композер (flash-тост). ④ Картка: «📣 Опублікувати» → «✍ Редагувати» (за скріном Олега); ORIGIN_LABEL += takes «🧵 тейк», idea «💡 з ідеї». Розміри фото 1:1/4:5/16:9 підтверджено оптимальними (4:5 = макс. площа стрічки IG і валідний для всіх мереж; 9:16 - тільки для сторіз/рілсів, окремий трек).
- **🧪 НА БЕТІ: 🧹 UX-батч (фідбек Олега: медіа-дублі, Чорновики, календар, План і ритм).** ① **Медіа-дублі - КОРІНЬ:** `ensureIgSafeImage` плодив копію при КОЖНІЙ IG-публікації (AI-картинки = PNG → копія завжди) - тепер ДЕДУП через `external_id`=ім'я оригіналу (reuse при повторних публікаціях); `GET /api/media` ховає source='ig-safe'; lifecycle `sweepLegacyIgSafe` зачищає старі копії без external_id; **масове видалення**: `POST /api/media/bulk-delete` + режим «☑️ Вибрати кілька» у Медіа-бібліотеці (клік=виділити, Вибрати всі, 🗑 Видалити обрані). ② **Чорновики (6 пунктів):** фільтри рубрика/джерело = 2 компактні дропдауни (було 2 ряди чіпів); «До календаря (N) →» переїхала в ЗАКРІПЛЕНИЙ ряд вкладок праворуч (sticky top:52px - місце для кнопки «наступний крок» на всіх вкладках); «Згенерувати ще» прибрано; топбарна «Згенерувати» → «＋ Додати матеріал» (видима з БУДЬ-ЯКОГО розділу, `openAddMaterial`); **ФІКС: 📣 композер не відкривався** - TDZ-баг з батчу 26 (`ov.querySelector('#cmpDel')` ДО `const ov`; node --check це не ловить - runtime) - блок перенесено після створення ov; 📣 тепер підписана кнопка «📣 Опублікувати»; у футері композера + «✅ Затвердити» (зберігає і затверджує, показує стан). ③ **Календар «Тиждень» = погодинна сітка** (як Typefully): колонка годин 00-24 + 7 днів, вертикальний скрол (візьме ~600px, автоскрол до поточного часу), чіпи стоять на своєму часі (колізії зсуваються вниз), лінія «зараз» у сьогодні, **дроп ставить день І ЧАС (крок 15 хв)**, теми-привиди плану - у смузі «план» зверху; місяць без змін; мобільний - горизонтальний скрол сітки. ④ **План і ритм:** скелет+ритм у 2 колонки, довгі пояснення → «?»-бейджі з tooltip (.qh); «📅 Контент-план по каналах» переїхав зі Стратегії у вкладку «План і ритм»; **📡 Ритм: КІЛЬКА часів на мережу** («＋» додає input; channel_rhythm {times:[]}, легасі time підтримано) - placePost ротує часи між постами (Threads 2-3/день). ⑤ **Прев'ю гілки Threads у композері** як у Typefully: аватар + вертикальна лінія + частини-відповіді (клієнтська розбивка по абзацах; сервер при публікації ріже AI точніше) - видно лише при увімкненому «🧵 Гілкою». **Докрутка (фідбек «не зрозумів де воно»):** режим = помітний чіп «🧵 Гілкою» у рамці під каналами; при вмиканні ІНШІ мережі затіняються (серія їде лише в Threads, зняток вибору відновлюється при вимкненні); кнопка «#⃣ Нумерація» (channels.threads.number, threadsSplit(numbering) і промт тримається авторського поділу порожніми рядками); підказка «порожній рядок = нова частина».
- **🧪 НА БЕТІ: 🧵 Threads батч 2 (П4-П8) + розширена аналітика Threads (за скрінами конкурентів ThreadsDashboard/Typefully).** ① **П4 «тест→масштаб»:** `repeatVariant` (cheapModel: свіжий гачок, решта дослівно, guard довжини) - `POST /posts/:id/repeat` = дубль поста (тільки Threads) + schedule_slot на +48г; `expandTake` (gpt-4o: тейк → повний пост 700-1200 симв) - `POST /posts/:id/expand-thread` = чернетка з channels.threads={on,thread:true} → одразу композер. У ⋯-меню картки (група «Розвинути», якщо sent містить threads): «🔁 Повторити хіт» + «🧵 Розгорнути в гілку»; у дайджесті під breakout - кнопка «🔁 Повторити хіт через 48 год» (tgbot callback `rep:`). ② **П5 стартовий пакет:** `threadsStarterPack` (3 біо «проблема→рішення» ≤150 + пост-знайомство + закріп-пост з лід-магнітом) - `POST /threads/starter-pack` створює 2 чернетки; кнопка «🚀 Стартовий пакет» у Стратегії Threads → модалка з біо (⧉ копіювати) + відкриття чернеток. ③ **П6 розбір ніші:** `threadsNicheReview` (матеріали з Threads-джерел RSSHub ≤30 + власні топи post_metric → gpt-4o → 3-5 формул {name,formula,example} + 5 ідей у Банк) - `POST /threads/niche-review`; кнопка «🔍 Розбір ніші» → модалка з «💾 Запамʼятати як правило голосу» (reuse /voice-rules); людська помилка, якщо джерел нема. ④ **П8 стрік:** у дайджесті рядок «🧵 Стрік N дн.» (⚠ якщо сьогодні пусто) + кнопка «🧵 3 тейки зараз» (callback `takes_gen`). ⑤ **Аналітика:** post_metric += replies/reposts/quotes (alter; воркер metrics пише повний mediaInsights); threads.ts += `userInsights` (профіль за since..until) + `followerDemographics` (gender/age/country; потребує threads_manage_insights і ≥100 підписників - best effort); `GET /api/analytics/threads` (кеш 15 хв у settings_block threads_an_cache): 7д метрики + дельти vs попередні 7д, followers_count, таблиця 10 останніх постів (👁❤️💬🔁❝), інтервал постингу, стрік (за таймзоною воркспейсу), демографія. UI: панель «🧵 Threads» в Аналітиці (тайли з дельтами + таблиця + бар-чарти демографії), вантажиться async, схована якщо Threads не підключено. П7 (реплай-коуч) чекає на апрув threads_manage_replies. (П1-П3 з дослідження 6 транскриптів + доказова база Mosseri/алгоритм).** План П1-П8 узгоджено; Олег подає `threads_manage_replies` в App Review (розблокує П7 реплай-коуч). ① **Playbook 2.0** (`CHANNEL_PLAYBOOK.threads` + коротке правило в adaptForChannels): раннє залучення > підписники, розмова > трансляція, хук першим рядком, робочі формати (тейк/питання/список/провал/цифри/контр-теза), сейви>лайки, CTA=кодове слово чи питання, ЗАБОРОНА engagement-bait, лінк краще окремою відповіддю. ② **🧵 Гілки**: `threads.publish(+replyToId)` (reply_to_id на ВЛАСНІ пости працює з базовим пермішеном); `threadsSplit(ws,text)` (gpt-4o: root=гачок+обіцянка, «2/»«3/»… ≤450 симв, детермінований фолбек по абзацах) - publisher.ts публікує ланцюжком з ПОВНОГО майстер-тексту (не скороченої 500-версії), інтервал 1.5с; вмикається `threads_strategy.thread='auto'` (довгі >500) або чіпом «🧵 Гілкою» на пості (channels.threads.thread, прев'ю показує ~к-сть частин); **відкладена CTA-гілка**: таблиця `threads_reply_job` + `threads_strategy.cta_min` (30/60/180 хв) - текст детерміновано з cta_config.threads (кодове слово/лінк/дія), воркер шле відповіддю в root, коли пост уже розганяється. ③ **Тейки**: `generateThreadsTakes(ws,n)` (≤280 симв, паливо = Банк ідей + 3 останні записи щоденника, mix спостереження/контр-теза/питання/цифри/зізнання, source origin='takes' → чернетки channels={threads:on}); кнопка «🧵 5 тейків» у рейлі Студії (всім, не PRO); щоденна автопорція о 8:00 локального часу (за годину до дайджесту). Новий воркер **`threads-auto.ts`** (60с: CTA-джоби + щоденні тейки, дедуп settings `takes_last`). UI: підпанель «⚙️ Стратегія Threads» у Налаштування→Канали→Threads (видима лише при підключеному Threads; автозбереження threads_strategy JSON). Далі: П4 тест→масштаб, П5 стартовий пакет, П6 розбір ніші, П7 реплай-коуч (після пермішена), П8 стрік.
- **🧪 НА БЕТІ: 🧹 Студія UX - картка без «пульта пілота» (фідбек Олега: багато мілких кнопок).** ① **Картка**: замість 12 іконок лишились ✅ Затвердити · 📣 Опублікувати · 🗑 (лише неопубл.) · ▶️ (рілс з відео) · **⋯-меню**; всі AI-інструменти в ⋯ з ПІДПИСАМИ й підказками, групами «Покращити» (↻ Переробити, 🎯 Директор) / «Розвинути» (🔥 Продовження, 🧲 Магніт, ♻️ COPE-PRO) / «Рілс» (🎞/▶️/📤, PRO) / «Інше» (🎨 Зображення, ⧉ Копіювати); на мобільному ⋯ відкривається шторкою знизу (.cardmenu bottom-sheet ≤700px). ② **Видалення замість архіву** (він не повертається до архівних): `DELETE /api/posts/:id`; ОПУБЛІКОВАНИМ - відмова 409 (усі *_publish і post_metric каскадяться від post - видалення стерло б історію публікацій і аналітику; вони живуть у фільтрі «Опубліковані»); 🗑 на картці, в композері (ховається якщо є sent) і в масових діях (bulk архів → bulk видалення зі скіпом опублікованих). Ендпоінт archive лишився в коді (legacy). ③ **Фільтр «✈️ Опубліковані»** у Студії: /posts/studio тепер віддає `sent[]` (6 publish-таблиць одним батчем); на опублікованій картці - статус-пілюля «✈️ Опубліковано» + `sentDots` (кольорові кружечки мереж КУДИ реально поїхало, + youtube/tiktok) з ✓; на затверджених/чернетках - chanDots ОБРАНИХ каналів (як і було). ④ **Композер зонами**: «🤖 ПОМІЧНИКИ ТЕКСТУ» (✍ 🪝 🔍 #) / «🖼 МЕДІА» (🎨 Зображення переїхало з ряду текстових) / футер «ЧЕРНЕТКА | ПУБЛІКАЦІЯ» з розділювачем + 🗑.
- **🧪 НА БЕТІ: 🎬 Motion-рендер рілсів (Remotion) - редакторський вигляд замість «ffmpeg-субтитрів».** `server/remotion/` (React-композиція `Reel.tsx`: анімований хук-заголовок зі spring-словами + акцентна риска; «караоке»-титри - слово, що звучить, на жовтій плашці, сказане біле, майбутнє пригашене; прогрес-бар угорі; CTA-плашка з voice_signature ≤30 симв; фони = ТІ САМІ пер-сегментні кліпи нашого конвеєра) + `src/remotion-render.ts` (бандл-кеш на процес, renderMedia concurrency=1, chromePath: REMOTION_CHROME→chromium→playwright-shell). Гілка в `buildReelVideo` за настройкою **`reel_renderer`='motion'** (селект у Бренд→Візуал, PRO): сегменти тимчасово публікуються в /media (Chromium тягне з 127.0.0.1; на беті /media без PIN), збій → авто-фолбек на класичний ffmpeg-шлях. Dockerfile: +`chromium` (apk) + `COPY remotion` + ENV REMOTION_CHROME. beta-check: смоук REMOTION (реальний 2с-рендер у контейнері; на сервері OK 16s) + пряма діагностика chromium. ⚠ АЛЬПІНА-ГРАБЛІ: Remotion запускає браузер через АБСОЛЮТНИЙ шлях `/bin/setpriv --pdeathsig`, а це BusyBox-заглушка → «Failed to launch browser / Closed with 1» без пояснень; фікс = `apk add setpriv util-linux-misc` + `ln -sf /usr/bin/setpriv /bin/setpriv`. Deps: remotion/@remotion/bundler/@remotion/renderer 4.0.489 + react18. E2E перевірено локально (Playwright headless-shell): 7с рілс ~1.5хв, монтаж-скрін надіслано Олегу; **ФІКС у композиції: пробіл-вузли між словами-спанами обовʼязкові, інакше рядок титрів не переноситься** (той самий клас бага, що й SVG-tspan в оверлеї). Ліцензія: безкоштовно ≤3 людей; при рості - Automators $0.01/рендер (мін.$100/міс).
- **🧪 НА БЕТІ: 📅 Хаб «Публікація» (план+календар разом) + 📡 ритм каналів зі спадкуванням (модель Buffer: один контент-план, різні розклади каналів).** ① **UI-зшивка:** Публікація = вкладки «📅 Календар | 📋 План і ритм»; `layPlan` ФІЗИЧНО переїхав з Створення у `pubPlanHost` (вузол той самий - обробники живі); вкладку «План» зі Створення прибрано, `setCTab('plan')` → редірект у Публікацію; біля календаря лічильник тем + кнопка «⚙️ План і ритм». ② **Привиди-теми в календарі:** слоти плану (empty/matched/drafted) рендеряться пунктирними чіпами `.pchip.ghost` («📋 тема» / «✍️ чернетка») на своїх датах - видно, що це ЩЕ НЕ пост; клік = згенерувати пост із теми (`/plan/slots/:id/generate`, from=material якщо є метч) або відкрити чернетку в композері. ③ **Ритм каналів** (панель «📡» у Плані): settings `channel_rhythm` JSON {net:{days:[0-6],time:'HH:MM',rubrics:[]}}; дефолт для всіх мереж - «як у бренду» (нуль налаштувань для простого юзера); оверрайд - дні-чіпи, час, фільтр рубрик. Бекенд: `schedule_slot.channels` jsonb (null = усі мережі поста, легасі); `/api/schedule/auto` → `placePost`: мережі-спадкоємці їдуть спільним слотом як раніше, кожна мережа з власним ритмом - ОКРЕМИМ слотом у свій найближчий день/час (+скіп, якщо рубрика поста не в її списку); `publishPostToChannels(ws,postId,onlyNets?)` + autopost передає підмножину слота; дедуп «раз на мережу» захищає від дублів; GET /schedule віддає coalesce(slot.channels,post.channels). PRO-шні канальні LLM-плани (planChanTabs) лишились як були (сховати - фаза 3).
- **🧪 НА БЕТІ: 📔 Щоденник - бот сам приходить по живий контент (ідея Олега + всі мої докрутки прийняті).** Нове `diary.ts`: воркер питає в DM **обід 13:00 / вечір 20:00** за таймзоною воркспейсу; питання НЕ копляться (liveSend категорії 'diary': нове видаляє старе); ротація 5+4 питань за днем місяця; вечірнє питання з **гачком під завтрашній слот плану** («завтра пост про X - маєш живий приклад?») і варіантом «додаси щось?», якщо вдень уже відповів; кнопка «🙅 Сьогодні нічого» (skip дня); **бекоф**: 3 дні пінгів без відповіді → обідній вимикається, лишається вечірній; `/diary` - питання негайно. **Захоплення**: текст при відкритому питанні (diary_state.pending) → запис дня (інакше, як раніше, Банк ідей); **голос → Whisper** (`transcribeVoice`, OPENAI_API_KEY, ogg) → текст із позначкою 🎙; **фото/відео/кружечок** → `tg.getFileBuffer` (ліміт Bot API 20МБ - дружня помилка) → `saveMedia(source='diary', external_id=запис)` + рядок у транскрипті. **Один запис на день**: source origin='diary', title «📔 Щоденник, D місяця YYYY», усе дописується `[HH:MM]`-рядками. Під підтвердженням кнопки: **✨ Зробити пост** (dpost) / **💡 Витягти ідеї** (dideas → story-режим Розвідника з 7 кутами) / **🎬 Рілс** (dreel, лише ПРО); у відео додатково «🎥 У b-roll» (dbroll → source='broll'). **Недільна петля** в дайджесті: «за тиждень N записів» + кнопки «💡 Ідеї з тижня» (dweek_ideas) і «🎞 Нарізка на рілси» (dweek_reels, ПРО і >800 симв). UI: мітка «📔 Щоденник» у фільтрах Матеріалів і на картках Студії; /materials/:id/ideas для origin='diary' → story-режим. Стан у settings_block `diary_state` {answered,skip,lunch,evening,pending,misses} - без нових таблиць.
- **🧪 НА БЕТІ: 🎓 Батч із курсу Caleb Ralston (бренд/контент/монетизація) - 4 блоки за пріоритетом Олега.** ① **ДНК бренду + сторі-промти:** нові settings `brand_assoc`/`brand_antiassoc`/`brand_story`/`brand_interests` + панель «🧬 ДНК бренду» у Бренд→Голос; `brandDna(s)` інʼєктується поруч із voicePassport у Lite (v2+legacy), rewritePost, extractIdeasFromText, reelsScript, sliceToReels (історія бренду = ЄДИНЕ джерело особистих фактів - AI не вигадує біографію); СТОРІ-фреймворк (хук→проблема→«брудна середина»→урок→CTA) + правило «конкретика замість епітетів» у меню фреймворків Lite v2; `OBJECTION_RULE` (закривай головний скепсис у тексті) у Lite; Розвідник story-режим 5→7 типів (+«шлях» origin-story, +«провал» помилка+урок); Директор: друга вісь ДОВІРИ (`trust:builds|spends` builds/spends + trust_why; анти-асоціації → verdict no), UI показує 🤝/💸. ② **Бенчмарки ×N (нове `metrics.ts` + таблиця `post_metric`):** воркер кожні 6г збирає insights опублікованих постів ≤90 днів (Threads views/likes, FB post_impressions, IG reach через нове `meta.igMediaInsights`; освіження раз на добу, 25/мережу/прохід); `networkBenchmarks(ws)` = МЕДІАНА по мережі (норма, ≥3 пости) + множник кожного поста; Аналітика: панель «📊 Пости відносно твоєї норми» (×2.0 зелений, ×<0.7 червоний) + кнопка **«🔍 Що спрацювало»** → `topPatterns` (gpt-4o шукає 1-2 патерни, що повторюються у ВСІХ топ-постах ×≥1.2, мін. 3) → модалка з готовим правилом → «Запамʼятати як правило голосу» (реюз /api/voice-rules Коваля). Дайджест: findBreakout тепер від медіани post_metric (фолбек - старе середнє); при ПРО у breakout додається кнопка **«🎬 Рілс із цього»** (tgbot callback `reel:` → reelsScript(30с) → чернетка format='reel' у run хіта). ③ **Контент-мікс 80/20 + 70/20/10 у скелеті плану:** `brand_interests` заповнено → кожен ~5-й слот = рубрика «Особисте» (теми з інтересів автора); кожен ~10-й слот = «🧪 експеримент» (префікс у темі; генерація з такого слота отримує інструкцію «подача, якої бренд ще не робив») - без нових колонок, інтент живе в theme/rubric. ④ **Сходи офферів:** settings `offer_low/mid/high` + панель «🪜» у Стратегії; `offerLadder(s)` → Lite (v2 у <business_goal> + legacy) і adaptForChannels: CTA за теплотою поста, «знання безкоштовно - продаємо виконання», продає історія клієнта з цифрами. Пропущено свідомо: командна секція курсу (юзери - соло; меседж для лендингу «сервіс = твоя контент-команда»).
- **🧪 НА БЕТІ: 🎬 Рілси = ПРО-трек (публікація IG/FB/YouTube/TikTok, вибір довжини, стокові фото Pexels, персональний b-roll).** ① **Все рілсове тепер ПРО-only:** 🎬 Сценарій/🎞 Нарізка на матеріалах, 🎞 збірка/▶️ плеєр/📤 публікація на картках Студії, панелі YouTube/TikTok у Каналах, b-roll-панель - `.proonly`/`if(PRO)`; updateProUI перерендерює materials+studio. ② **Вибір довжини ПЕРЕД генерацією** (`askReelLen`: ⚡до 15с / 🎯до 30с / 🎬45-60с) → `reelsScript/sliceToReels(ws,text,targetSec)`; `reelLenRule` диктує к-сть бітів (2/3/4-5). ③ **Публікація готового рілса** (📤 → модалка мереж → фонова джоба `startReelPublishJob` + полінг GET /reel-publish): **IG Reels** (`meta.publishReelToInstagram`: контейнер media_type=REELS + полінг status_code до FINISHED, до ~3хв), **FB** (`publishVideoToPage` file_url), **YouTube Shorts** (`youtube.ts`: Google OAuth offline на нашому GOOGLE_CLIENT_ID, scope youtube.upload, multipart upload + #Shorts; ⚠ Олегу: у Google Cloud Console увімкнути YouTube Data API v3 + додати redirect `/api/integrations/youtube/callback`), **TikTok** (`tiktok.ts`: OAuth v2 + inbox upload одним чанком - до аудиту застосунку відео падає юзеру в ЧЕРНЕТКИ TikTok; env `TIKTOK_CLIENT_KEY/SECRET`, без них «🕓 Скоро»). Підпис = `reelCaption` (cheapModel, кеш `channels.reel_caption`); дедуп `reelSentNetworks` (meta/youtube/tiktok_publish). Нові таблиці youtube_config/_publish + tiktok_config/_publish; channels/status +youtube/tiktok; OAuth-міст +?youtube/?tiktok. ④ **Стокові фото Pexels** (усім, не лише ПРО): «🖼 Зі стоку» у фото-редакторі → `stockPhotoOptions` (cheapModel → пошуковий запит → Pexels /v1/search, орієнтація за форматом) → 3 варіанти з фотографом → `attachStockPhoto` (URL-guard images.pexels.com) → кроп під формат, текст-оверлей працює. ⑤ **Персональний b-roll:** панель «🎥 Мої відео-вставки» у Бренд→Візуал (ПРО) → `POST /api/media?source=broll` (тільки відео; ліміт multipart 15→60МБ) → `brollPlan` у reelvideo: 1-3 власні кліпи на ролик у слоти ХУК/середина/CTA, пріоритет над Pexels-стоком. Свої відео НА Pexels заливати не треба - персональна бібліотека живе в нашій медіатеці.
- **🧪 НА БЕТІ: 🎨 Оверлей 2.0 (редакторські обкладинки за прикладами Олега) + 📅 календар тиждень/місяць.** ① **overlayHeadline v2** (images.ts, тепер export): `OverlayStyle` += align (left/center), upper, accent (hex; акцентне слово = у *зірочках* або останнє), kicker (надзаголовок letterspacing), subtitle, bg:'tint' (затемнення ВСЬОГО фото 0.45), size sm/md/lg; tspan-ранки згруповані за кольором + xml:space="preserve" (SVG колапсив пробіли між tspan - слова злипались!); запас ширини 30px (bold ширший за оцінку chW). UI-пресети в фото-редакторі: **Мінімал / Цитата (центр, великими, без фону) / Обкладинка (tint + жовтий акцент + кікер + підзаголовок) / Плашка** + селект акцентного кольору (жовтий/блакитний/рожевий/зелений); `/image-text` приймає всі нові поля. Монтаж 4 пресетів перевірено візуально (еталони: dashkiev-цитата, kaftanyk-обкладинки). ② **Календар:** режими **Тиждень** (7 високих колонок, повний текст постів ~70 симв) і **Місяць** (класична сітка, сусідні місяці приглушені `.dim`), навігація ‹ › + «Сьогодні», перемикач зберігається в `localStorage['kg_calmode']` (дефолт week); увесь dnd/✕/підсвітка сьогодні працюють в обох режимах; `#cal` тепер wrapper (controls + div.cal всередині).
- **🧪 НА БЕТІ: 📸 IG-постинг фікс + ручний кроп + календар (✕/перетягування).** ① **Корінь «проблеми з зображеннями» в IG:** gpt-image-1/Gemini повертають PNG, а Instagram API приймає ЛИШЕ JPEG з ratio 0.8-1.91 → `ensureIgSafeImage(ws,filename)` в images.ts (перевірка format+ratio; за потреби центр-кроп до найближчої допустимої пропорції + JPEG 90, копія source='ig-safe'); publisher IG-гілка проганяє КОЖНЕ фото через неї. Юніт 4/4. ② **Ручний кроп у фото-редакторі** (Олег: «обрізає за власним бажанням»): після вибору з галереї/завантаження - крок `cropStep` (рамка з пропорцією формату: drag pointer-події + повзунок розміру, box-shadow-затемнення довкола; «✂ Обрізати так» шле нормований crop{x,y,w,h}, «⚡ Авто» = старий attention-кроп); `attachCroppedImage` приймає crop → sharp.extract→resize fill; EXIF orientation 5-8 враховано (сторони міняються місцями). ③ **Календар:** ✕ на чіпі (крім posted) → confirm → DELETE /schedule/:id; заплановані чіпи draggable → дроп на інший день = PUT /schedule/:id (час зберігається, formato 'slot:<id>:<HH:MM>'); підсвітка «сьогодні». Ідеї далі (НЕ зроблено): навігація тижнями назад/вперед, зміна ЧАСУ при дропі (зараз лишається), місячний вид.
- **🧪 НА БЕТІ: UI без розробницького жаргону + вибір Threads-акаунта.** ① Підказки-хінти переписано для кінцевого користувача: Threads-панель (було «Потрібні threads_content_publish + …, акаунт у Threads Testers»), GDrive («scope drive.file»); статуси «APP_ID не налаштований» → «🕓 Підключення … тимчасово недоступне» (Threads/Meta/GDrive). Хінт «Власний бот» (BotFather) лишено - це реальна інструкція для юзера в розширених налаштуваннях. ② **Вибір Threads-акаунта:** Threads API НЕ віддає список акаунтів (токен = профіль, залогінений у threads.net) → перед OAuth модалка `openThreadsConnect`: «підключиться акаунт, під яким ти зараз у Threads» + кнопка «🔄 Перемкнути акаунт у Threads» (threads.net/settings, нова вкладка) + «✓ Підключити цей». Чесний еквівалент вибору сторінки FB/IG.
- **🧪 НА БЕТІ: «Скіли 2.0» - великий батч з ДОСЛІВНИХ промтів плагіна content-system-georgii-rivera (zip від Олега) + його акценти.** ① **Антидетектор 2.0:** `AI_TRACE_RX` розширено 13→37 патернів (порт humanizer-ru каталогу на укр: HARD BANS з підказками-замінами, маркетингові кліше, канцелярит, артефакти чат-бота, фінгерпринти 2025-26: тріади-резюме regex, емодзі-декор \p{Extended_Pictographic}, псевдо-емпатія); `HARD_BAN_HINTS` map; aiAudit LLM-промт = тонкі патерни (рвана медитативність, псевдо-сократика, псевдо-терапевтичний регістр, кардіограма ритму); **`deAiFix`** = ТОЧКОВА правка (замінює лише знайдені фрагменти, решта дослівно; guard довжини 0.5-1.6) + 2 проходи; `POST /posts/:id/deai-fix`; кнопка в композері тепер точкова. Юніт 9/9 (брудний ≥12 знахідок, чистий 0). ② **Паспорт голосу:** нові settings `voice_address/voice_signature/voice_stoplist/voice_emoji` + `goal_metric`; `voicePassport(s)` інʼєктується в 6 точок генерації; панель «🪪 Паспорт голосу» в Бренд→Голос + бейдж «⚠ голос не відкалібрований»; `goalRule` тепер з метрикою і строком. ③ **Розвідник 2.0:** story-режим = 5 типізованих кутів (урок/контрхід/фреймворк/доказ/релейтбл, контрхід+фреймворк першими), signal = еталон «тейк vs шум» + перша ідея = найшвидший виграш; вивід +`format` (пост/карусель/рілс, бейдж у модалці). ④ **Хук-майстер 2.0:** `suggestHooks` → {culmination, hooks[3], soft-місток, remove[]}; модалка показує кульмінацію, 🕊 місток і ✂ артефакти. ⑤ **Сценарист:** еталон біта (multishot) + блок «ТЕКСТ НА ЕКРАН» (титри-акценти); `parseReelScript` СКІПАЄ цей блок (не потрапляє в озвучку). ⑥ **Мультиплікатор:** **«Нарізка»** `sliceToReels` (транскрипт >800 симв → 5-7 самостійних сценаріїв з «чому сам по собі» + порядок на тиждень; `POST /materials/:id/reel-slices`, кнопка «🎞 Нарізка на рілси»); «Серія» тепер з АРКОМ (частини повʼязані містками, остання = пейофф з CTA). ⑦ **Директор:** verdict +`sharper` (гостріша версія - UI пропонує замінити перший абзац) + лічильник промахів `director_misses`. ⑧ **Магніт 2.0:** +attach (привʼязка до реальних постів юзера - топ-8 у промт), +promo-хук, +quick (⚡ швидкий виграш рівно один); **`buildLeadMagnet`** «✍ Зібрати» → ГОТОВИЙ текст магніта чернеткою в Студії; **«магніт під тему»** `POST /posts/:id/lead-magnet` + кнопка 🧲 на картці (модалка: Зібрати / → вплести CTA в пост). ⑨ **Дайджест ← аналітика:** `findBreakout` (Threads insights останніх ≤6 постів за 72г; топ ≥1.5× середнього і ≥30 переглядів) → «🔥 пост залетів» + кнопка «5 кутів продовження» (tgbot callback `dev:` → suggestDevelopment → Банк ідей); рядок «🎯 N чернеток повз ціль». ⑩ **Рілс-інфраструктура:** `post.format` ('post'|'reel' alter), сценарії/нарізка пишуться як format='reel', бейдж «🎬 рілс» на картці Студії; ПУБЛІКАЦІЯ рілсів у IG/FB/TikTok/YT = наступний трек (IG Reels через Graph API container REELS - реально; TikTok/YT - окремі апруви). ⑪ adaptForChannels: еталон CTA (multishot) + «TG: перший рядок чіпляє до згортання» + LinkedIn-скелет утримання; хінт «наступного звена» після публікації.
- **🧪 НА БЕТІ: ⭐ AI-оцінка матеріалів + фільтр за стрічкою + чистий фолбек Google News (фідбек Олега «текст = сирий google-URL»).** ① **Чистий фолбек:** якщо стаття не тягнеться (Cloudflare 403), тіло матеріалу = заголовок ОДИН раз + «Джерело: видавець» + ДЕКОДОВАНЕ посилання видавця (resolveGoogleNewsUrl тепер викликається ДО fetchArticleText; сирий google-URL більше не пишеться). ② **Фільтр за конкретною стрічкою:** `source.feed_id` (alter, FK content_source on delete set null), інжест пише feed_id, `/api/materials` віддає feed_id/feed_title/feed_url + ai_score; у Матеріалах другий ряд чіпів-стрічок (📸 @user / ✈️ t.me/x / назва теми; клік = toggle, комбінується з фільтром типу). ③ **⭐ Оцінка цікавості (1-10):** `scoreMaterials(ws,items)` у pipeline.ts - ОДИН виклик `google/gemini-2.5-flash` (free tier; без ключа - через OpenRouter) на батч нових матеріалів: score+why з контексту бренду/аудиторії/брифу → `source.ai_score/ai_score_why` (alter); викликається у фоні після інжесту + `scoreBackfill()` при старті поллера (останні 40 без балу, ≤10/воркспейс). UI: бейдж «⭐ N/10» на картці (≥8 зелений жирний, 5-7 амбер, <5 сірий; tooltip = чому).
- **🧪 НА БЕТІ: 📸🧵 Instagram- і Threads-сторінки як джерела (обрані користувачем чужі акаунти).** ① **Instagram = офіційний шлях, НЕ скрейпінг:** `meta.businessDiscovery(igUserId,pageToken,target)` (Meta Graph business_discovery) - читає пости будь-якої публічної БІЗНЕС/КРЕАТОР-сторінки через ПІДКЛЮЧЕНИЙ Instagram воркспейсу; вимога: юзер спершу підключив свій IG (інакше людська помилка); особисті акаунти API не віддає (теж людська помилка). `content_source.kind='instagram'`, url=маркер `instagram:username` (НЕ http). Поллер: гілка `fetchInstagramItems` (external_id=`ig:<mediaId>`, content=caption+permalink; догрузка статті вимкнена - підписи самодостатні). ② **Threads через RSSHub** `/threads/:user` (kind='rss', як Telegram). ③ Резолвер: типи 'instagram'/'threads', `extractHandle` (@user / instagram.com/user / threads.net/@user), `resolveSource(...,workspaceId)`; POST /sources/rss приймає kind. UI: 2 нові опції в дропдауні, `rssNiceUrl` показує 📸 @user / 🧵 @user замість внутрішніх маркерів. beta-check: +RSSHUB_THREADS (@zuck живим). ⚠️ IG-джерело живе поки живий page_token юзера; Threads-роут RSSHub - скрейпінг threads.net (може флапати, бекоф поллера прикриє).
- **🧪 НА БЕТІ: 📡 RSS Фаза 2 (Telegram-джерела) + 💼 LinkedIn-постинг (5-та мережа) + 2 фікси Google News з фідбеку Олега.** ① **Фікс сирого HTML у матеріалах** (Олег: «<a href=…» в тексті): `decode` у rss.ts розекранував сутності ПІСЛЯ зрізання тегів → екранований Google News-опис виживав як текст; тепер порядок CDATA→сутності→теги→другий прохід сутностей (подвійне екранування &amp;nbsp;); юніт-тест на точному пейлоаді Олега; `cleanBrokenItems()` при старті поллера одноразово чистить уже зіпсовані матеріали в БД. ② **Догрузка тексту статті**: Google News дає лише заголовок+джерело → на інжесті айтемів <180 симв: `resolveGoogleNewsUrl` (декодер: старий формат = URL у base64-id; новий AU_yq = сторінка статті з SOCS/CONSENT-куками → підпис data-n-a-sg/ts → внутрішній batchexecute; ПЕРЕВІРЕНО живим: повний URL epravda витягнуто) → `fetchArticleText` (og:description + <p>≥60 симв, кап 6к) → матеріал = заголовок+стаття; видавці за Cloudflare (403 з датацентр-IP, напр. epravda) → фолбек заголовок+клікабельне посилання. ③ **RSSHub у compose** (образ diygod/rsshub БЕЗ chromium, `expose`-only - жодних портів назовні, mem_limit 400m; env `RSSHUB_BASE_URL` дефолт http://rsshub:1200) → тип «✈️ Telegram-канал» у «Додати джерело»: @user / t.me/user → `/telegram/channel/:user` (публічні канали, без API/логіну); картка стрічки показує ✈️ t.me/user замість внутрішнього URL. ④ **LinkedIn**: `linkedin.ts` (OAuth openid profile w_member_social; escape Little Text Format; POST /rest/posts LinkedIn-Version 202506; двокроковий upload зображення - LinkedIn НЕ приймає image-by-URL, читаємо з MEDIA_DIR), таблиці `linkedin_config`/`linkedin_publish`, гілка в publishPostToChannels + alreadySentNetworks, adaptForChannels правило+ліміт 3000, `/api/integrations/linkedin[/connect|/callback|/disconnect]`, channels/status.linkedin, `/api/published` union. UI: NETS+NETVAR(--li #0A66C2)+NETICON+NETLIM 3000+NETFOLD 210 «…more», панель LinkedIn у Налаштування→Канали (статус/підключити/перепідключити при протуханні 60-денного токена), OAuth-попап bridge `?linkedin=ok`. **Токен живе 60 днів БЕЗ програмного рефрешу (базовий доступ) → UI-індикатор перепідключення. Кнопка зʼявиться після апруву застосунку LinkedIn (env LINKEDIN_CLIENT_ID/SECRET); до того статус «🕓 Скоро»; Олегу: створити застосунок у LinkedIn Dev Portal, продукти «Sign In with LinkedIn using OpenID Connect» + «Share on LinkedIn», redirect `https://socialio.rozum.one/api/integrations/linkedin/callback` (+beta.), submit на апрув.** beta-check: +ARTICLE_TEXT (жива екстракція) +RSSHUB_TG (@telegram через rsshub) + `docker compose ps rsshub` + `free -h`.
- **🧪 НА БЕТІ: 📡 RSS Фаза 1 (за спекою Олега `rssservicespec.md`; Фази 2-4 - RSSHub/Telegram-джерела, LinkedIn-постинг - ПОПЕРЕДУ).** Спека = standalone-агрегатор; наш маппінг: айтеми стають Матеріалами (не «читалка»), тому будували лише відсутнє. ① **Резолвер+прев'ю** (`rss-resolver.ts`): `POST /sources/rss/resolve {type:news|rss,input,lang}` - «Новини за темою» будує Google News RSS (`news.google.com/rss/search`, hl/gl/ceid uk/en/cs, БЕЗ ключа), посилання → прямий фід з автопошуком (/feed,/rss,/feed.xml,/rss.xml,/atom.xml); людські помилки (приватний/недоступний/таймаут). `rss.ts`: +`parseFeedTitle` (channel/feed title, зрізає item-блоки), `fetchFeedRaw`. Юніт-тест 8/8 (Google News CDATA + Atom фікстури). ② **UI-флоу «Знайти → прев'ю → Підключити»**: дропдаун типу (📰 Новини за темою / 🔗 RSS URL) + мова, картка «Знайдено: … останні 5 постів», «Підключити» зберігає title+url і одразу тягне перші пости (`/pull`); картки стрічок показують title жирним + бейдж **«⚠ джерело недоступне»** (tooltip = last_error). ③ **Поллер**: експоненційний бекоф битих фідів (15хв×2^n, стеля 6г; `content_source.error_count` alter) + jitter 0.5-3с між фідами; успіх скидає лічильник; ручний ↓ ігнорує бекоф. ④ beta-check воркфлоу тепер сміок-тестить живий Google News з сервера (`GNEWS_RESOLVE`). Рішення по спеці: LinkedIn-читання НЕ робимо (RSS.app платний, цінність мала); X/IG/TikTok-скрейпінг відкладено; LinkedIn-автопостинг = окремий трек (заявку в LinkedIn Dev Portal подати завчасно - апрув тижні; код піде за шаблоном threads.ts як 5-та мережа).
- **🧪 НА БЕТІ: 4 фікси з фідбеку колеги Олега (тестував прод).** ① **Математика плану:** `buildLiteSkeleton` розкладав слоти ТІЛЬКИ по best_days (`candidates[i%len]`) - при ppw > best_days/тиж пости стакались на ті самі дати, підсумок виглядав випадковим (16/36). Тепер: слоти РІВНОМІРНО по горизонту без дублювання дат (best_days = «магніт» ±2 дні), друге коло тільки коли target > днів; cap 60→120; юніт-тест 7/7 (30дн×4=17, 30дн×10=43, 90дн×14=120). UI: живий «≈ N постів» біля полів днів/п-тиж. ② **📏 Формат постів у мережах** (`settings_block.channel_format` {net:{len:short|std|long,note}}): панель у Налаштування→Канали (під CTA); `adaptForChannels` інʼєктує цільову довжину (short=50-150 симв 1-2 речення - кейс «Threads під тренди») + нотатку стилю з пріоритетом над плейбуком. ③ **Автоперепаковка «створи раз - сервіс сам пакує»:** `publishPostToChannels` перед відправкою сам адаптує мережі БЕЗ власної версії тексту (1 виклик на всі відсутні; збій → майстер-текст як раніше; покриває autopost і бота); композер «Опублікувати зараз» адаптує ДО публікації й показує в прев'ю. ④ **Календар «поз'їжджав»:** `.cal` grid `1fr`=minmax(auto,1fr) → чіп з довгим словом розтягував колонку (репро в Chromium: 224px vs 106px). Фікс: `repeat(7,minmax(0,1fr))` + `.pchip{min-width:0;overflow-wrap:anywhere}`.
- **🧪 НА БЕТІ: 🖼 Двокроковий фото-редактор («Instagram на мінімалках») + фікс z-index галереї.** `openPhotoTool` повністю переписано: **крок 1 - джерело** (формат 1:1/4:5/16:9 + 3 кнопки поруч: 📎 З галереї / ⬆ Завантажити / 🎨 Згенерувати; промт розкривається під «Згенерувати») → **крок 2 - текст** (прев'ю; «🔁 Перегенерувати» з полем «що змінити» - коментар доклеюється до промта, лише для AI-фото; заголовок + ✨ Підказати; **стилі тексту: шрифт sans/serif/mono · місце top/center/bottom · фон затемнення/плашка/без фону (з тінню)**; «✍ Накласти текст» безкоштовно на базі, «Прибрати текст», «← Інше фото», «Готово»). Якщо фото вже є - відкривається одразу крок 2. `openImageEditor` (картка Студії) тепер делегує в `openPhotoTool` - один редактор всюди. **Бекенд:** `images.ts` `OverlayStyle` (position/font/bg; DejaVu-шрифти є в контейнері) - `overlayHeadline` малює градієнт за позицією / заокруглену плашку / тінь-«без фону»; нове `attachCroppedImage(ws,postId,mediaId,aspect)` - **галерейне/завантажене фото ОБТИНАЄТЬСЯ під формат** (sharp cover+attention, rotate() шанує EXIF) і кроп-копія стає `image_base` → накладання тексту працює і на НЕ-AI фото; `POST /posts/:id/media {mediaId,aspect}` → кроп, `/image-text` приймає {position,font,bg}. **Баг-фікс:** `chooseMedia` z-index 70→96 (відкривався ПІД фото-модалкою 90). Всі 9 комбінацій стилів перевірено sharp-тестом локально (монтаж-скрін).
- **🧪 НА БЕТІ: 4 фікси з тестування Олега.** ① **Рілс не губиться:** `post.reel_video` (alter) - після збірки mp4 пишеться на пост; GET /reel-video має фолбек на БД (переживає рестарт); на картці зʼявляється **▶️** назавжди; замість window.open (блокувався браузером після async-полінгу) - **вбудований плеєр-модалка** (`openVideoModal`: video+download). ② **Антидетектор докручено:** `scanAiTraces` - детермінований regex-каталог 13 патернів (тире, «не просто X, а Y», канцелярит, кліше-гачки; УВАГА: `\b` не працює з кирилицею → lookbehind/lookahead), LLM шукає лише тонкі патерни; знахідки зливаються. Кнопка «🔍 AI-сліди» тепер **сама виправляє** одним confirm (PUT + regenerate з інструкцією-переліком слідів). Юніт-тест: 8/8 на брудному, 0 на чистому. ③ **Тире вбито в корені:** `stripDashes` у `chat()`+`geminiChat()` (openrouter.ts) - «—»/«–»→« - » на виході КОЖНОГО LLM-виклику (промпти лишились, але гарантія тепер кодом). ④ **Threads 500 симв.:** adaptForChannels - жорсткі ліміти всіх мереж у промпті + перевірка КОДОМ після генерації: перевищив → повторний виклик «скороти до N», досі довший → зріз по слову з «…».
- **🧪 НА БЕТІ: 🎞 ПРОТОТИП «сценарій → готовий відео-рілс» (за результатами deep-research: Azure укр TTS = верифіковано найдешевша основа).** Новий `reelvideo.ts`: `parseReelScript` (ХУК/БІТи/CTA + [візуал:]) → Azure TTS per-сегмент (`uk-UA-PolinaNeural`, env `AZURE_SPEECH_VOICE`; REST `tts.speech.microsoft.com`, mp3 24kHz) → тривалості через ffprobe → сток-кліпи Pexels (portrait, keywords 1 викликом CHEAP_MODEL; фолбек: картинка поста з zoompan → градієнт) → FFmpeg: сегменти 1080x1920 → concat → мукс з аудіо → **вшиті укр. субтитри (ASS, DejaVu Sans)**. Джоба фонова (`startReelJob`/`reelJobs` Map): `POST /posts/:id/reel-video` стартує, `GET` полить (nginx-таймаути не страшні), mp4 падає в MEDIA_DIR. UI: кнопка **🎞 на картці сценарію** (контент починається з 🎬) → полінг → відкриває відео. Dockerfile: +`ffmpeg font-dejavu` (runtime). env: **`AZURE_SPEECH_KEY/REGION/VOICE`** (обовʼязково; F0 free tier), **`PEXELS_API_KEY`** (опційно). FFmpeg-збірку+ASS+кирилицю перевірено локально e2e (скрін кадру). Потрібно від Олега: ключ Azure Speech у .env бети.
- **🧪 НА БЕТІ (ще не в main): Мультиплікатор + Магніт + Коваль (скіли 6, 9, 10; 7-CTA і 8-Антидетектор були зроблені раніше).** ① **Мультиплікатор:** кнопка «⚡ Серія (~6 постів)» на матеріалі → `POST /materials/:id/series` (тейки Розвідника → `generatePostsOnePass`, кожен пост - окремий кут+гачок); кнопка **🔥 на картці Студії** («Продовження») → `POST /posts/:id/develop` (`suggestDevelopment`, CHEAP_MODEL) → 5 кутів розвитку теми → `idea_bank` (origin='ai'). ② **Магніт:** панель «🧲 Лід-магніти» у Стратегії → `POST/GET /api/lead-magnets` (`suggestLeadMagnets`, gpt-4o, кеш у `settings_block.lead_magnets`) → 3 конкретні магніти {title,what,leadgen,effort,keyword}; кнопка «→ CTA для Instagram» пише кодове слово в `cta_config`. ③ **Коваль (самонавчання голосу):** після «Переписати» з інструкцією (композер + модалка ↻) — confirm «Запамʼятати як постійне правило голосу?» → `POST /api/voice-rules` дописує `- правило` в `tone_of_voice` (guard 4000 симв.). На прод — після тесту на беті.
- **🧪 НА БЕТІ (ще не в main): Розвідник + Хук-майстер + Архітектор + Сценарист (скіли 2-5 контент-плагіна).** ① **Розвідник:** `extractIdeasFromText` тепер видає ТЕЙКИ `{idea,angle,hook,rubric}` (не голі теми) з режимами за походженням матеріалу: RSS → «Сигнал» (що бренд каже від себе), нотатка/ідея → «Історія» (кути кейсу); модалка ідей показує 🪝 гачок + 🎯 кут, у генерацію йде повний тейк. **Петля «Питання»:** після успішної публікації (publish-all + autopost) у фоні `publishQuestions` → 3 питання-продовження в `idea_bank` (origin='ai', guard ≤40 new). ② **Хук-майстер:** кнопка «🪝 Гачок» у композері → `POST /posts/:id/hooks` (`suggestHooks`, gpt-4o) → модалка з 3 відкриттями з кульмінації, клік замінює перший абзац. ③ **Архітектор (непересічення):** «✨ Підказати» в редакторі зображення → `POST /posts/:id/headline` (`suggestHeadline`, CHEAP_MODEL) — заголовок ДОПОВНЮЄ пост, не дублює перший рядок. ④ **Сценарист:** кнопка «🎬 Сценарій Reels» на матеріалі → `POST /materials/:id/reels` (`reelsScript`, gpt-4o) → готовий до зйомки сценарій (ХУК 0-2с → 4-5 бітів з [візуал:] → CTA) як чернетка в Студії. На прод — після тесту на беті.
- **🧪 НА БЕТІ (ще не в main): Директор + CTA-маршрутизація + хук/анти-AI правила (батч 1-3 з контент-плагіна).** ① **🎯 Головна ціль** (`settings_block.primary_goal`: money/leads/growth/authority/quality) — чіпи в Стратегії (`goalChips`); `goalRule(s)` інʼєктується в Lite (v2 `<business_goal>` + legacy), PRO-кроки, adaptForChannels, rewritePost, extractIdeasFromText. **Кнопка 🎯 на картці Студії** → `POST /posts/:id/director` (`directorVerdict`, CHEAP_MODEL) → вердикт yes/partial/no + причина + правка → confirm застосовує правку через `/regenerate`. ② **📮 CTA-конфіг** (`settings_block.cta_config` JSON {net:{type:link|keyword|action,value}}) — панель у Налаштування→Канали (`ctaRows`); `adaptForChannels` вплітає механіку кожної мережі (кодове слово для IG тощо), правило «один заклик = одна дія». ③ **HOOK_RULE** (бан кліше-гачків + відкриття з кульмінації) + **ANTI_AI_RULE** (укр. каталог AI-слідів) вшиті поряд із NO_DASH_RULE у всі 5 точок генерації. ④ **Кнопка «🔍 AI-сліди» в композері** → `POST /posts/:id/ai-audit` (`aiAudit`, CHEAP_MODEL) — діагностика без правки, список патернів із цитатами. Все additive; на прод піде після тесту на беті (merge beta→main).
- **🧪 БЕТА РОЗГОРНУТА І ЖИВА (2026-07-09).** `provision-beta.yml` (запуск: push у гілку `provision-beta`; workflow_dispatch недоступний токену сесії) сам розгорнув усе на сервері: клон `/opt/socialio-beta` (гілка beta), .env бети (копія прод + бета-рядки, новий пароль БД згенеровано на сервері), стек `socialio-beta` (health 8081 OK), nginx vhost, **SSL до 2026-10-07 з автопродовженням**. https://beta.socialio.rozum.one → PIN 2345. Прод не зачеплено.
- **🧪 БЕТА-середовище (beta.socialio.rozum.one, PIN 2345).** Гілка `beta` + `deploy-beta.yml` (push у beta → `/opt/socialio-beta`, graceful skip поки сервер не розгорнуто). Compose: порт параметризовано `${APP_PORT:-8080}` (прод без змін); ізоляція бети через `COMPOSE_PROJECT_NAME=socialio-beta` у її .env (свої контейнери/БД/volumes). **PIN-гейт** у server.ts за env `BETA_PIN`: сторінка «Введи PIN» → кукі 30 днів (sha256(pin+secret)); відкриті без PIN `/health`, `/api/webhooks/*` і `/media/*` (Telegram/Meta тягнуть картинки по URL при публікації); rate-limit 10 спроб/хв. **`TELEGRAM_WEBHOOK_OFF=1`** — initTelegramBot не реєструє webhook (спільний бот: публікація з бети працює, DM-фічі обробляє прод; інакше бета вкрала б webhook). Бейдж **BETA** в app.html за hostname. Шпаргалка разового розгортання: `server/BETA.md` (DNS → клон+.env → compose up → nginx+certbot). Цикл: фічі→beta→тест→merge у main.
- **📱 Реалістичне прев'ю соцмереж у композері (нативний вигляд + «… ще»/«Показати повністю»).** `renderPrev` переписано: кожна мережа рендериться як у реальному застосунку — **Instagram**: фото зверху → рядок дій (♡💬↗🔖) → `хендл підпис…` з обрізанням; **Threads/Facebook**: текст зверху → фото знизу; **Telegram**: фото+повний текст (канал не згортає). Візуальний згин `NETFOLD` (instagram 125 / facebook 280 / threads 320 симв.) з клікабельним `NETMORE` («… ще» для IG/FB, «Показати повністю» для Threads) — розгортається на місці (`_pvExp` Set). Лічильник символів `len/NETLIM` лишився (червоний при перевищенні жорсткого ліміту); для Telegram з фото понад 1024 — примітка, що підпис піде окремим повідомленням. Аватар-ініціали + хендл у шапці, пілюля-лейбл мережі над карткою. Перевірено рендером у Chromium (Playwright).
- **💸 Бюджетний маршрут на Gemini 2.5 Flash для дешевих кроків (фічафлаг, за замовч. off).** `openrouter.ts` `chat()` тепер має гілку **`google/*` → Gemini** (Google AI Studio `generativelanguage`, свій формат запиту/відповіді, облік у `llm_usage`); якщо нема `GEMINI_API_KEY` — падає в OpenRouter (він теж уміє google/gemini-*). Новий env **`CHEAP_MODEL`** (`env.cheapModel`, дефолт `openai/gpt-4o-mini`): 5 низько-ставкових кроків (`plan_themes`, `ideas`, `hashtags`, `plan_match`, `insights`) ходять через нього. Постав `CHEAP_MODEL=google/gemini-2.5-flash` у `.env` → ці кроки йдуть на безкоштовний Gemini. **НЕ чіпав** фундамент (генерація постів gpt-4o, derive_brand/strategy на gpt-4o-mini) і **розподіл по календарю** (`/schedule/auto` — детермінований, взагалі без LLM). `GEMINI_PRICES` = 0 (цільовий free-tier). `.env.example` доповнено (OPENAI/GEMINI/CHEAP_MODEL). ⚠️ Приватність: free-tier Gemini може використовувати дані для тренування — для проду з клієнтськими даними бажано платний тариф.
- **🐛 Ранкове зведення: 2 баги + дія (діагностовано з реального дайджесту юзера).** ① **Мова:** інсайт генерувався `output_language` бренду (у юзера - чеська: «Zaměřte se na vizuály…») поряд з укр. каркасом → мікс. Тепер `generateInsightPool` завжди **українською** (owner-facing), + ключ кешу `insight_pool`→`insight_pool_v2` (скидає старий чеський пул, перша генерація вже укр). ② **Пасивний текст:** зведення казало «нема поста на завтра», але дії не давало (лише лінк). Додано кнопку **«✍️ Зробити пост зараз»** (`slot_post:<slotId>`) — бере найближчий порожній/зметчений слот, генерує чернетку прямо в DM з кнопками Опублікувати/Переробити (`slotToPost` у tgbot = дзеркало `/api/plan/slots/:id/generate`). + рядок «✍️ Найближча тема: …». ③ **`/digest`** — команда надіслати зведення негайно (перевірка без очікування 9:00; `sendDigestNow` експортовано, tgbot↔digest цикл безпечний у ESM).
- **🔗 Кнопка «Підібрати матеріали» в Плані.** Поряд зі «Згенерувати скелет» — `planMatchBtn` → `POST /api/plan/match` (`matchPlanSlots`) → перезавантажує план. Закриває розрив: якщо матеріали з'явились ПІСЛЯ побудови скелета (метчинг авто-йде лише на generate/add-material/rss), слоти лишались «порожньо»; тепер юзер оживляє план на вимогу (порожньо → 📎 є матеріал). Уся решта логіки Плану (статуси matched/drafted/approved/scheduled/published, кнопки, тип-бейдж=рубрика) вже була реалізована — це лише ручний тригер метчингу.
- **✍️ «Переробити» під чернеткою в DM (докрутка бота).** Під згенерованою в боті чернеткою тепер ряд кнопок `✅ Опублікувати` / `✍️ Переробити` / `📋 Ще ідеї` (хелпер `draftButtons`). Callback `rw:<postId>` = `rewritePost(ws, content)` (парафраз у голосі бренду) → оновлює `post.content` і **редагує те саме повідомлення на місці** (`editMessageText`), не плодячи нових. Повний цикл рецензування в DM: чернетка → переробити (скільки треба) → опублікувати. Планування з DM (tz-коректний слот) — окрема наступна докрутка.
- **☀️ Ранкове зведення + пул інсайтів + нагадування по плану (Фаза 1b бота).** Новий воркер **`digest.ts`** (`startDigest` у listen, tick кожні 5 хв): для кожного `tg_owner` о **9:00 локального часу воркспейсу** (таймзона з `settings_block.timezone`, дедуп через `settings_block.digest_last=<дата>`) шле ОДИН живий меседж (category 'daily'): **інсайт дня** + «⚠️ завтра нема поста» (перевірка `plan_slot` на завтра) + «💡 N ідей у Банку» + «📭 нема плану» → кнопки `💡 Показати ідеї` (idea_list) / `⚡ Сформувати план` (plan_gen) / 🌐 застосунок. `pipeline.ts`: **`nextInsight(ws)`** — пул ~30 інсайтів у ніші бренду (1 виклик), кешується в `settings_block.insight_pool` {items,cursor}, тягне по одному (=> ~1-2 виклики/міс). `tgbot.ts`: `liveSend` експортовано; callback **`plan_gen`** = `buildPlan` (Lite-скелет 14дн×4, як `/api/plan/generate` lite). Циклів імпорту нема (digest→tgbot/pipeline; tgbot не імпортує digest).
- **📣 Публікація з DM (Фаза 1a бота, крок 2b).** Під згенерованою в боті чернеткою тепер кнопка **«✅ Опублікувати в Telegram»** (`pub:<postId>`): вмикає `channels.telegram.on` на пості й кличе наявний `publishPostToChannels` → рапортує «✈️ Опубліковано» або чітку помилку (нема каналу → інструкція підключити). `ideaToPost` повертає `{id,content}`. Повний цикл у боті: думка → `idea_bank` → чернетка → публікація в канал, без заходу в застосунок.
- **🤖 Telegram DM-асистент (Фаза 1a бота, крок 2 — жива-меседж-інфра + захоплення ідей + /idea).** Нові таблиці **`tg_owner`** (tg_user_id→workspace, для проактивних DM + атрибуції; закріплюється при `/start <code>` з кабінету; фолбек на `tg_connect` для вже підключених) і **`tg_message`** (один живий меседж на категорію). `telegram.ts`: inline-клавіатури (`TgButton`/`sendMessage(...,buttons)`), `deleteMessage`, `editMessageText`, `answerCallbackQuery`; `setWebhook` тепер слухає `callback_query`. `tgbot.ts`: `liveSend(ws,chat,category,text,buttons)` (гасить старе повідомлення категорії → шле нове → зберігає message_id — так у чаті не накопичуються меседжі); **захоплення ідей** — будь-який текст у DM падає в `idea_bank` (origin='bot') + підтвердження з кнопкою «✨ Зробити пост»; **`/idea`** — живий список банку з кнопкою-постом на кожну; `handleCallback` роутить `idea_post:<id>` (ideaToPost = source→run→`generatePostsOnePass`, ставить used) і `idea_list`. Канал-конект (forward/@username) збережений. Публікація з DM (approve/schedule) — наступний крок 2b.
- **💡 Банк ідей (Фаза 1a бота, крок 1 — дані+UI, без бота поки).** Нова таблиця **`idea_bank`** (workspace-scoped: text/angle/rubric/origin[manual|ai|bot|plan|material]/status[new|used|archived]/used_post_id) — постійний банк концептів постів, окремо від run-bound `idea`. Застосовується авто при деплої (schema.sql через `migrate.js` на старті, `create table if not exists`). Endpoints: `GET/POST /api/ideas`, `POST /api/ideas/:id/archive`, `POST /api/ideas/:id/post` (робить source→run→`generatePostsOnePass([text])`, ставить status='used'+used_post_id). UI: у стрічці **Матеріали** доданий фільтр-чіп «💡 Банк ідей» (`renderIdeaBank`) — список ідей із бейджами (🏷 рубрика / 🤖 з Telegram / ✨ AI), кнопки «✨ Пост» і «🗑», «＋ Додати ідею». Це фундамент під `/idea` в боті; наповнення ботом (origin='bot') — наступний крок. AI-модалку «Ідеї з матеріалу» НЕ чіпав (щоб не плодити дублі).
- **🧭 Персист під-вкладки Створення + поле «постів/тиждень» у Lite + прибрано легенду статусів.** ① Оновлення сторінки більше НЕ стрибає завжди на «План»: `setCTab` пише під-вкладку в `localStorage['kg_ctab']`, init відновлює її (materials/plan/posts), і лише для першого разу (нема збереженого) відкриває План якщо скелет є (раніше рядок init безумовно робив `setCTab('plan')`). Разом із `kg_view` оновлення тепер лишає юзера рівно там, де він був. ② Поле **постів/тиждень** (`planPpwWrap`) розховане з `proonly` → видиме і в Lite; `buildLiteSkeleton(ws, horizon, postsPerWeek)` тепер рахує к-сть слотів як `round(горизонт/7 × ppw)` і розкладає їх по best_days (замість жорсткого best_days×perDay), сервер `/plan/generate` (lite-гілка) передає `posts_per_week`. Тепер «14 днів × 4/тиж = 8 постів» — і це керовано. ③ Прибрано текстову легенду статусів `⬜ порожньо → 📎 → ✍️ → ✅ → 🗓 → ✈️` з панелі Плану (візуальний шум; кольорові бейджі статусів на слотах лишились).
- **🧭 Розділ памʼятається між оновленнями сторінки.** `selectView` тепер пише активний вид у `localStorage['kg_view']`, а init на старті відновлює його (валідний зі списку 7 розділів, інакше «Створення») замість жорсткого `selectView('create')`. Раніше будь-яке оновлення (напр. після години простою) скидало юзера на «Створення». OAuth-редіректи (`?meta/threads/gdrive`) виконуються ПІСЛЯ відновлення й коректно перекривають вид. Уся навігація (десктоп `.navitem`, мобільна, `go()`) іде через `selectView`, тож персист єдиний.
- **🐛 Telegram double-post fix:** `publisher.ts` тепер дедуплікує `chat_id` у циклі публікації (`sentChats` Set). Раніше цикл слав окремо в `channel_chat_id` і `group_chat_id` без перевірки — якщо той самий чат опинявся в обох полях (напр. forward-connect оновлює лише `channel_chat_id`, а старий `group_chat_id` = той самий чат лишається), приходило ДВА однакові пости. Один фізичний чат тепер отримує пост рівно один раз за публікацію. (Доповнює per-network `alreadySentNetworks`, який уже не дублює МЕРЕЖУ.)
- **🐛 КОРІНЬ бага «план 8, але список порожній»: колізія імен `renderPlan`.** Стара конвеєрна `renderPlan` (рендерила `#o7`/`S.plan`) стояла ПІСЛЯ нової (скелет `#planList`) → через hoisting перемагала стара, тож `applyPlanFilter()` викликав НЕ ту функцію, `#planList` лишався порожнім (а бейдж «2/8» ставив окремий `updateCounts` — звідси розбіжність). Стару перейменовано на `renderLegacyPlan` (+ виклик у `applyState`). **Знайдено браузерним аудитом** (Playwright + Chromium, замоканий API): проходить Чорновики/План/композер/мобільну версію й робить скріни; підтверджено, що після фіксу План показує слоти, композер відкривається (4 чіпи каналів + прев'ю-телефони, # Хештеги/Підлаштувати працюють), поп-ап «Налаштування генерації» перед ідеями рендериться, мобільна нижня навігація видима без горизонт. скролу. Єдина колізія імен у файлі — саме `renderPlan` (перевірено grep-ом усіх дубльованих функцій).
- **📱 Мобільна версія (mobile-first) + фікс «план є, але не видно».** ① **Баг плану:** старі слоти мали `channel='telegram'` (створені ДО переходу Lite на `channel='all'`), а Lite-фронт фільтрував `?channel=all` → бейдж 8, список порожній. Фікс: `loadPlan` тепер тягне `/plan` БЕЗ фільтра → `PlanAll`, фільтрація клієнтська (`applyPlanFilter`): Lite показує ВСІ слоти (легасі — з тегом каналу), PRO — вкладки `Спільний`(якщо є)+4 мережі; `renderPlan` захищений від null theme; Lite-генерація на бекенді чистить незаповнені слоти БУДЬ-ЯКОГО каналу (легасі-скелети). ② **Мобільна версія:** viewport-fit=cover; **нижня фікс-навігація** `.bottomnav` (5 розділів, ті самі `data-view`/`selectView`, safe-area env()); топбар на мобільному = 1 компактний рядок (таби сховані); контент `padding-bottom` під нижню панель; заборона горизонтального скролу (`overflow-x:hidden`, img max-width); `.tabs` скроляться; модалки `max-height:100dvh` + скрол; календар 7 днів — горизонтальний скрол у панелі; композер: кнопки внизу на всю ширину + safe-area; `#bulkBar` підіймається над нижньою навігацією. Стару мертву @media для сайдбара замінено.
- **✍ Композер + Чорновики + публікація один раз на мережу (batch D+E+F).** **D (Пости→Чорновики):** вкладка «📝 Пости» → «📝 Чорновики», заголовок Студії «На перегляд» → «Чорновики», генерація говорить «чорновиків». Модель уже правильна: `generatePostsOnePass` кладе `stage='final'` з ГОЛОСОМ+де-AI (buildLitePrompt v2) як канало-незалежний майстер-чернетку; per-network варіанти йдуть з `adaptForChannels` у `channels` jsonb. `stage='final'` — внутрішня назва стадії конвеєра (не «опубліковано»), лишена як є (інакше зачепить studio/autopost/publisher запити). **E (повноекранний композер):** новий `openComposer` = overlay `.cmp-ov` на весь екран, 2 панелі з незалежним скролом. Ліворуч: селектор каналів угорі, select рубрики, велика textarea майстер-тексту, dashed-кнопки `# Хештеги` / `⚡ AI фото` / `✍ Переписати`. Праворуч: мобільне прев'ю на кожну обрану мережу + лічильник символів під ліміт (`NETLIM` telegram 1024, threads 500, instagram 2200, facebook 2000, червоний при перевищенні). Низ: 💾 Зберегти чернетку · ✨ **Підлаштувати під канали** (перейменовано з «під мережі») · дата/час · 🗓 Запланувати · 📣 Опублікувати зараз. `openPhotoTool` (галерея/завантаження/AI-промт + пропорції **1:1/4:5/16:9** + опційний заголовок-оверлей). Нові ендпоінти: `POST /posts/:id/hashtags` (`suggestHashtags`), `/image` приймає `{aspect,prompt}` (images.ts: `Aspect`, `OPENAI_SIZE`/`FAL_SIZE`, `overlayHeadline` тепер за реальними розмірами картинки), `/full` віддає `rubric`+`image_prompt`, `PUT /posts/:id` пише й `rubric`. **F (публікація один раз на мережу):** `alreadySentNetworks(postId)` (publisher.ts, з *_publish status='sent'); `publishPostToChannels` ПРОПУСКАЄ вже надіслані мережі (status `skipped`) — і для ручної, і для планової публікації (schedule на ІНШІ мережі не дублює); `GET /posts/:id/publish-state` → `{sent}`; композер блокує чіп надісланої мережі (✓, disabled) і показує «✓ Опубліковано» у прев'ю; publish-all гасить запланований слот ЛИШЕ коли не лишилось ненадісланих мереж; autopost трактує `skipped` як benign (не fail); адаптація завжди з майстер-чернетки (`/adapt` бере `post.content`). Клік на запланований слот у календарі відкриває композер із `{slotId}`.
- **🗂 План-скелет Lite/PRO + чистий Lite «Пости» + попап налаштувань генерації (batch A+B+C).** ① **План більше не порожній:** Lite тепер будує ДЕТЕРМІНОВАНИЙ канало-незалежний скелет (`buildLiteSkeleton` у pipeline.ts: рубрики×best_days×теми зі стратегії, теми — 1 дешевий gpt-4o-mini з фолбеком `рубрика: ідея дня`, ніколи не порожньо якщо є стратегія; `channel='all'`). Раніше падало бо `generateChannelPlan` (LLM) флапав `[]`. ② **PRO — 4 вкладки каналів** у Плані (`planChanTabs`, PlanChan): кожен канал має власний LLM-план (`generateChannelPlan`, `channel=<net>`). `/api/plan/generate` розгалужується на `mode:'lite'|'pro'`; `/api/plan?channel=` фільтрує + повертає `{slots,channels}`. `planChannelWrap`/`planPpwWrap`/`planChanTabs` = `.proonly`; `updateProUI` перезавантажує план при Lite↔PRO. ③ **Lite «Пости» очищено:** «Налаштування генерації» + «Як це зроблено» тепер `.proonly` (лишаються тільки в PRO). Налаштування генерації показуються **попапом ПЕРЕД ідеями**: `openMaterialIdeas` = 2 кроки (⚙️ кількість+рубрики → 💡 список ідей, кнопка «← Налаштування»); `extractIdeasFromText` приймає `rubricsFilter`, ендпоінт `/materials/:id/ideas` шле обрані рубрики.
- **🧭 Menu reorg: контент у меню, «сантехніка» під аватаром.** Головна навігація 7→5 (Створення · Публікація · Бренд · Стратегія · Аналітика). **Аватар (О)** = дропдаун: email (+⚡ коли PRO), перемикач Lite/PRO (segLite/segPro переїхали сюди), тема, «Мова застосунку» (disabled-заглушка під мультимовність) + пункти Профіль/Канали/Джерела + Вийти; ⚡-бейдж на аватарі при PRO. **Налаштування = 3 вкладки** (`sTabs`/`setSTab`): Профіль (таймзона, тариф, акаунт, небезпечна зона) · Канали (Telegram, Threads, FB/IG - вихідні) · Джерела (транскрипт-textarea, медіа, GDrive, RSS, останні джерела + Fireflies-панель - вхідні). Стара секція `data-view="sources"` видалена, вміст фізично переїхав у вкладку; `selectView('sources')` шимиться → settings+вкладка (task strip / gdrive callback працюють). **Бренд = 2 вкладки** (`bTabs`): ✍️ Голос (приклади+derive, ToV, аудиторія, де-AI, мова виводу) · 🎨 Візуал (провайдер зображень + image_style, переїхали з Налаштувань; дубль-панель прибрано). **Нотатки контент-стратегії (`strat`/content_strategy) переїхали в розділ Стратегія.** Дубльованих id немає, node --check чистий.
- **🏗 Plyn v3: конвеєр Матеріали → План → Пости (дизайн-хендофф Олега).** **Оболонка:** сайдбар → верхня навігація (logo + 7 табів + score pill + сегмент Lite/PRO + «Згенерувати» + тема + аватар/вихід); сегмент = ПРОСТЕ перемикання (upsell-модалку прибрано; тарифи будуть поповненням токенів). **Створення = 3 вкладки:** 📥 Матеріали (стрічка sources з фільтрами по типах, розгортання з повним текстом, дії: ✨пост / 💡ідеї-модалка з чекбоксами й рубриками / 🗑прибрати(archive), бейдж метчу «🏷 підходить: рубрика · дата», «＋ Додати матеріал»-модалка) · 📅 План (СКЕЛЕТ: прогрес «заповнено X з Y», статуси ⬜→📎→✍️→✅→🗓→✈️, генерація скелета прямо тут (канал/днів/п-тиж → POST /api/plan/generate), дії слота: «✨ З матеріалу»/«З теми»/«→ до поста»/«→ календар») · 📝 Пости (стара Студія; PRO бачить під-перемикач Студія/Конвеєр). Дефолт вкладка: план є → План, нема → Пости. Панель «Джерело» з рейлу видалено. **Бекенд:** таблиця `plan_slot` (workspace-scoped: slot_date, channel, rubric, theme, hook, cta, status, match_source_id, post_id) + `source.archived`; ендпоінти `/api/plan/generate|/api/plan|/api/plan/match|/api/plan/slots/:id/generate{from:theme|material}` + `/api/materials|/:id|/:id/archive|/:id/ideas|/:id/posts`; `matchPlanSlots()` (gpt-4o-mini метчинг слоти×матеріали; авто після plan/generate і rss ingest) + `extractIdeasFromText()`; генерація з теми створює source origin='plan' (тег «📅 з плану»); статуси слота синхронні з життєвим циклом поста (review approved/archived, /api/schedule, publish-all, autopost); **/api/schedule/auto ставить планові пости САМЕ на дату їх слота** (перший час зі стратегії), решту - по best_days. **v2 = ДЕФОЛТ рушій промтів** (`prompt_engine!=='legacy'`; тумблер «🧠 Система промтів» видалено з UI; legacy лишився в коді як аварійний через API). Панелі брифу/канальних планів у Стратегії тепер proonly (v2only клас видалено).
- **🧠 Prompt quality upgrade (Anthropic-style techniques from Oleg's prompt library, v2-gated where generation-affecting).** ① `buildLitePrompt` v2-гілка повністю переписана в **XML-структуру** (`<role>` елітний копірайтер → `<strategy_brief>` → `<brand>` → `<voice_examples>` → `<rules>` → `<task>` → `<output_format>`); **multishot**: реальні `voice_examples` юзера (до 3000 симв.) інʼєктуються як еталон голосу (раніше Lite бачив лише стислий опис!); **меню гачків** (6 типів: цікавісний розрив/патерн-перебій/контр-теза/помилка/число/обіцянка) + «склади 3, залиш найсильніший»; само-критика за 5 критеріями. Legacy-гілка не змінена (флаг off = стара поведінка). ② `deriveVoice` (глобально, безпечно - пише лише в `tone_of_voice_derived`): структурований вивід - тон/звертання + 5 РОБИ + 5 НЕ РОБИ + фірмова лексика + політика емодзі (формат brand_voice з бібліотеки). ③ `adaptForChannels` при v2: інʼєктує `<strategy_brief>` + ПОВНІ `CHANNEL_PLAYBOOK` (замість однорядкових правил) + само-критика + модель gpt-4o (замість mini); legacy = як було.
- **🛠 Big fix+UX batch (rollback point = branch `claude/rollback-2026-07-02` = 48fc9df; revert = force-push it to main).** **Bugs:** ① double-posting killed on 3 paths - publish-now гасить planned-слоти поста; POST /api/schedule реюзає наявний planned-слот (move, не другий INSERT); PUT /api/schedule/:id більше не «воскрешає» posted-слоти (тільки planned/failed, інакше 409. ② Calendar off-by-one: сітка тижня тепер за TZ воркспейсу (полудень-UTC якір), не браузер+toISOString. ③ Telegram formatting: `toTgHtml()` (** __ ~~ `) + parse_mode HTML з фолбеком на plain при parse-помилці. **Quick wins:** favicon.svg (з meta-review іконки) на всіх сторінках; повноекранна анімація «Налаштовую кабінет» при завершенні онбордингу; ВСІ широкі тире (—) вичищені з UI/промтів + `NO_DASH_RULE` вшитий у 5 точок генерації (Lite/steps/adapt/rewrite); техполя Telegram (токен/chat id) сховані в «⚙️ Розширені налаштування», .env-жаргон прибраний. **Images:** генерація тепер ЗАВЖДИ чиста (без тексту) - заголовок юзер підтверджує в редакторі і накладає через /image-text (явний headline в /image все ще накладає одразу); нове поле `image_style` (ToV для картинок, Налаштування → 🎨) інʼєктується в промт; онбординг шле `provider:'gemini'` (Nano Banana) для перших зображень; чекбокс image_overlay прибраний з UI (легасі-ендпоінт лишився). **UX core:** `post.rubric` (schema alter) штампується Lite-генерацією (+rubric у JSON-відповіді); /api/posts/studio віддає rubric+source_origin; Студія: композитні фільтри статус×рубрика×джерело (Publer-модель) + теги на картках + чекбокси й **масові дії** (затвердити/архів, плаваюча панель); вкладка Інбокс прибрана (код дормант); блок «Ідеї→пости» тепер PRO-only (`.proonly` в updateProUI); rss-poller auto_run → `generatePostsOnePass` (Lite) замість 6-крокової кишки. **Regen+analytics:** «↻ Переробити» відкриває модалку «що саме змінити» → rewritePost(instruction) поверх повного контексту (бриф v2+ToV+deAI); Аналітика: скелетон одразу, 4 локальні запити паралельно, meta/stats довантажується async у #igLive (не блокує сторінку).
- **🧠 Prompt-engine v2 (Strategy Brief chained system) — behind a flag, fully reversible.** New `settings_block.key='prompt_engine'` (`v2`|`legacy`, default legacy). Settings toggle «🧠 Система промтів». ALL v2 logic branches on the flag; legacy code paths untouched → flip OFF = instant revert, no redeploy. Four steps, all in `pipeline.ts` (no schema migration — uses `settings_block` for storage): **(1)** `generateStrategy()` branches to `generateStrategyV2` → richer SUPERSET JSON (keeps rubrics/best_days/times/channels so calendar+rubrics still work) + brief fields (positioning/icp/pillars/brandscript/voice/messaging/offers); compact text rendered via `renderBriefText` into `settings_block.strategy_brief`. **(2)** `buildLitePrompt` injects the brief + copy-framework picker (AIDA/PAS cold, BAB short, FAB/4P warm) + self-critique when v2. **(3)** `generateChannelPlan(ws,channel,horizon,ppw)` (`CHANNEL_PLAYBOOK` = telegram/instagram/threads/facebook only, 2025-26 algo signals) → JSON plan rows saved to `settings_block.channel_plan_<channel>`. Endpoints `POST /api/channel-plan` + `GET /api/channel-plan/:channel`. **(4)** `atomizePost(ws,content,channels)` (COPE/Prompt 10) → `{atoms,matrix}`, display-only (no DB writes). Endpoint `POST /api/posts/:id/atomize`. UI (app.html, all gated by `window._v2` / `.v2only`): brief panel + content-plan panel in Стратегія; ♻️ atomize button + modal on Studio cards. Source: Oleg's "Universal SMM Prompt Library" (Anthropic XML-chaining + Hero-Hub-Hygiene + 80/20 + StoryBrand). **Skipped from the library:** TikTok/YouTube playbooks (socialio doesn't publish there), case-study/KPI prose (landing/analytics, not code).
- **Fireflies webhook fixed:** root cause = HMAC secret mismatch (401). User set matching secret → signature passes. Follow-up `07a99be`: tolerant meetingId (meetingId/meeting_id/id/data.*) + test-pings (no meetingId) return 200 + log payload (was 400, risked Fireflies disabling the webhook). Config: secret_set+auto_run+api_key all set. «Без підпису» (token-only) button remains as fallback. **Meta App Review (Marketing Pulse 1255): queue cleaned to exactly 7 (Chrome-Claude), use-cases filled; PENDING user: screencasts + agree checkboxes + reviewer instructions + Submit.**
- **Meta App Review prep:** confirmed root cause of external-connect error = App Review NOT submitted (perms Standard-only). Built the demonstrable bits: `instagram_content_publish` added to META_SCOPES (`f54fb25`, IG publish for non-admins); `meta.igInsights` + Analytics IG card (justifies `instagram_manage_insights`); `/data-deletion` page (Meta requirement); 1024² app icon. Full submission guide + per-permission use-cases + screencast script in **`meta-review/APP-REVIEW.md`**. Redirect URI to verify in FB Login settings: `…/api/integrations/meta/callback`. **User submits App Review (button) + records screencast; meanwhile add external users as Testers.** **Threads = SEPARATE app `1347525417441376` (≠ Marketing Pulse 1255…) → its own App Review for 3 perms: `threads_basic`, `threads_content_publish`, `threads_manage_insights` (we publish + read post insights). The threads_* in the Marketing Pulse queue are NOT socialio's (Holos). Verified META_APP_ID=1255606142995192 vs THREADS_APP_ID=1347525417441376.**
- **Студія/Інбокс decoupled from the active source/run** — now a GLOBAL list of ALL workspace final posts via `GET /api/posts/studio`; `loadStudioPosts()` sets `Finals` (applyState no longer overwrites it from the run; switching source keeps the post list; postAction/image-editor refresh the global list not the per-run `refresh()`). The source/run is now only the generation context (ideas + new posts append to the global pool).
- image EDITOR shipped: prominent «🎨 Зображення» editor (studio card + composer) — generate vs cheap re-overlay-text on a saved base image; post.image_base/headline; /image-text endpoint. + `CLAUDE.md` project-memory file (d0e6411).
- `d71554e` media thumbnails (`/thumb/:name`, sharp, disk cache) · `a7c5093` ideas selectable cards · `a21a418` re-derive brand overwrites on IG switch (+any language) · `ee37d55` multi-provider transcribers (Fireflies/Grain/MeetGeek) · `b866753` gamification phase 2 (task strip) · `5eb6ec0` tariffs page (fake billing) · `67ec64c` gamification phase 1 · `2533955` Threads posts images · `8e3a6a5` Studio ideas→posts block · `5ce64e4` Telegram photo posts + shared bot · `a63b9bc` brand-from-Instagram + drop hardcoded defaults · `496a7eb` onboarding popup lines.
- `21f9a23` text overlay (sharp+SVG) · `ee1e8e1` AI image gen 3-provider · `f2d4dfb` Lite/PRO UI · `331111f` OAuth popup + account picker · `9982392` OpenAI-direct · `705db54` reset button · `ef450f0` Lite single-pass · `fca38db` IG-first onboarding · `9653ec5` instant first posts · `828258b` timezone · `de70c3b` dead-code cleanup · `ceb4868` publishing visibility · `cd7bbb4` account lifecycle · `4132c5e` Plyn redesign.

## graphify

This project has a knowledge graph at graphify-out/ with god nodes, community structure, and cross-file relationships.

Rules:
- For codebase questions, first run `graphify query "<question>"` when graphify-out/graph.json exists. Use `graphify path "<A>" "<B>"` for relationships and `graphify explain "<concept>"` for focused concepts. These return a scoped subgraph, usually much smaller than GRAPH_REPORT.md or raw grep output.
- If graphify-out/wiki/index.md exists, use it for broad navigation instead of raw source browsing.
- Read graphify-out/GRAPH_REPORT.md only for broad architecture review or when query/path/explain do not surface enough context.
- After modifying code, run `graphify update .` to keep the graph current (AST-only, no API cost).
